# 决策记录 (DECISIONS)

本文件以流水账方式记录项目中的关键决策与原因，避免日后反复纠结同一问题。
新决策追加到末尾，不删除历史。

---

## 2026-06-30 · D1：文档体系与同步约定

**背景**：项目希望沉淀文档，使每个新会话能"冷启动"快速理解项目，且后续每次改动文档与代码同步更新，保证不跑偏。

**决策**：建立以下文档，并约定同步纪律。

文档体系：
- `CLAUDE.md`（项目根，⭐ 新会话自动加载）：项目概览、技术栈、如何运行、目录结构、关键约定、指向其它文档的链接。
- `docs/specs/<日期>-travel-journal-design.md`：需求 & 设计文档（头脑风暴产物）。
- `DECISIONS.md`（本文件）：关键决策流水账。
- 实现计划 `docs/plans/<日期>-...-plan.md`：设计定稿后由 writing-plans 技能生成。
- `README.md`：可选，暂缓（私人项目，先靠 CLAUDE.md）。

**同步纪律**：每次改动，相关文档与代码一起更新。
- 数据模型变化 → 更新设计文档 + `CLAUDE.md`
- 新的取舍/选择 → 追加到本文件 `DECISIONS.md`
- 需求变化 → 更新设计文档

---

## 2026-06-30 · D2：项目定位与技术栈（讨论中，待设计定稿确认）

- 定位：**纯私人**旅游记录网站，无账号系统、无多用户。
- 运行：**本地单机网页**（不部署云端），未来可从本地升级到云端。
- 技术栈：Python + Flask + SQLite + Jinja2 模板（服务端渲染，极少 JS）。
- 存储：SQLite 单文件（要做花费统计，优于 JSON）。图片存本地 `uploads/`，库中存路径。
- 配图定位：**少量配图**，以文字记录为主。

> 注：D2 为讨论阶段结论，正式细节以设计文档定稿为准。

---

## 2026-06-30 · D3：核心数据模型定稿

设计文档：`docs/specs/2026-06-30-travel-journal-design.md`。

- **同行人 Person 独立成表、可复用**（姓名+照片），与 Trip 多对多。原因：同一人会出现在多次旅行。
- **城市 City 独立成表、可复用**，含经纬度，为第二版地图展示做准备；坐标自动地理编码。
- **行程段 Leg 建模**：有序的「出发城市→到达城市 + 出行方式」。原因：用户的路线本质是分段、有顺序、每段有交通方式（如 北京→飞机→香港→游轮→冲绳…），trip 级的"出行方式多选"画不出路线，且以后补做迁移成本高，故现在一步到位。
  - 推论：Trip 的城市/出行方式由 Leg 推导，**砍掉 TripCity 关联表**。
  - Leg 不加日期字段，仅靠 seq 排序。
- **Leg 与 Day 不直接关联**，通过 City 间接相连。两层分离：Leg=怎么走，Day=每天干啥。原因：低耦合，地图与日常记录互不影响。
- **多币种**：每个旅程可声明多个外币，各自手动设汇率；**汇率方向 = 1 人民币 = ? 外币**；换算 `人民币 = 外币 ÷ 汇率`；不自动取汇率。
- **Entry 五类**：吃饭/游玩/购物/住宿/交通。原因：住宿、交通是旅行大开销，纳入才能完整统计。
- **同天记录按添加顺序**排列，不加时间字段、不做拖拽。YAGNI。
- **移动日**（游轮在途等）简单处理：自选一个城市，不设"在途"特殊类型。

## 2026-06-30 · D4：范围与版本

- **第一版**：记录流程 + 单旅程统计（饼图按类别、柱状图按天、按币种）；旅程页仅列表概要。
- **第二版**：旅程首页地图/地球路线展示。原因：先跑通基本流程，地图作为增强。
- 统计**只做单旅程**，不做跨旅程全局统计。
- 菜单：一级 `旅程`(含 创建/统计) ｜ `设置`(同行人/城市)。

---

## 2026-06-30 · D5：技术落地

第一版实现完成后的技术细节记录。

- **ORM**：Flask-SQLAlchemy（模型层），用 `db.get_or_404` 的 2.0 形式避免 LegacyAPIWarning。
- **地理编码**：OpenStreetMap Nominatim（免费无 API key），失败返回 None。
- **图表**：Chart.js（base.html CDN 引入），实时渲染统计数据。
- **金额**：`Numeric` 列 + Python `Decimal`，每条目四舍五入两位（ROUND_HALF_UP），各汇总口径一致。
- **时间戳**：`created_at` 用 timezone-aware `datetime.now(timezone.utc)`（避免 utcnow 弃用警告）。
- **Flash 消息**：需要 `SECRET_KEY`（已加入 config，env 可覆盖）。
- **币种下拉**：记录花费时只能选「CNY + 本旅程已声明外币」，to_cny 对未配置币种有防御性 0.00 兜底（UI 约束，正常不可达）。
- **编辑旅程路由**（最终审查）：`/<trip_id>/edit` 路由复用 `_apply_form` 与 `form.html`（已支持编辑模式）；`add_entry` 增加 `day.trip_id != trip_id` 完整性校验，不匹配时返回 404。

---

## 2026-06-30 · D6：数据库演进策略 & 文档更新标准

**背景**：补全文档体系，明确「冷启动 / AI 接手」需要的沉淀，以及各文档的更新规则；同时把一个隐藏坑（无 migration）定下处理方式。

### 数据库 schema 演进（无 migration）
- 现状：用 SQLAlchemy `create_all` 自动建表，**无 Alembic/migration**。
- 决策：**纯私人单机项目，改模型时直接删 `instance/travel.db` 重建**（数据是测试/个人数据，可承受丢失）。不引入 migration 框架（YAGNI）。
- 注意：一旦库里有想保留的真实旅行数据，再改 schema 前需**手动备份/迁移**；届时若频繁改模型，可重新评估引入 Alembic。

### 文档体系（沉淀什么 & 给谁看）
四类文档，更新规则**由类型决定，不是每份每次都改**：

| 文档 | 类型 | 更新规则 |
|------|------|----------|
| `docs/specs/*-design.md` | **状态快照** | 改数据模型/页面/统计/需求时同步到与现实一致；上线或移除**功能**时更新「功能总览（当前已实现）」节。状态型文档过时即是 bug。 |
| `CLAUDE.md` | **入口/地图** | 改项目结构（新增顶层目录/模块）、技术栈、运行方式时更新。含目录结构与约定，必须名副其实。 |
| `DECISIONS.md` | **追加日志** | 有新取舍 / 踩坑 / 推翻旧决策时**追加新条目**，不改旧条目（推翻则新条目里说明 supersede 了哪条）。 |
| `docs/plans/*` | **一次性产物** | 某版本的实施计划，做完即视为归档，**不再维护**。 |

### 不该碰文档的情况
- 改 bug、调样式、加测试、重命名内部变量 → **只靠 commit message 记录**，不更新文档。
- 强行「每次迭代都更新所有文档」会制造**假同步**，反而有害。

### 一次迭代的文档自检（合并前）
对照上表：改了模型/页面/统计/需求→动 spec；改了结构/运行→动 CLAUDE.md；有新决策→追加 DECISIONS；上线/删功能→改 spec 功能总览节；其余→不动。

### 暂不做
- `README.md`：仍暂缓（私人项目，CLAUDE.md 已覆盖入口职责）。
- `CHANGELOG.md`：不单独维护，git log + 规范 commit message（`feat/style/docs/fix` 前缀）已足够。

---

## 2026-06-30 · D7：实时汇率 & 旅程表单首次引入前端 JS

**背景**：优化「创建行程」——行程段/币种从固定行数改为动态增删，并希望选币种时自动带出汇率，省去手查。

**决策**：
- **汇率源**：`open.er-api.com`（免费、无需 key）。其 `latest/CNY` 返回的 `rates[code]` 即「1 人民币 = ? 外币」，**正好等于本项目既有汇率语义**，无需换算方向。失败/未知币种/本币返回 None，前端静默兜底由用户手填。
- **分层**：查汇率走 `services/exchange.py`（同 geocoding 风格、可 mock 测试），蓝图加只读 JSON 端点 `GET /trips/exchange-rate?code=XXX`；前端不直连第三方（避免 CORS、便于测试、汇率源可随时替换）。
- **前端 JS**：本项目首次引入客户端 JS（`static/form.js`，原生、零依赖）。仅负责旅程表单的行级增删与选币种取汇率；用 `<template>` 克隆空行，事件委托处理增删，至少保留一行。其余页面仍保持服务端渲染、无 JS。
- **汇率自动填充**：选币种后自动写入汇率输入框，**允许手动覆盖**（输入框可编辑）；编辑旅程时已存在的币种行保留库内汇率，不自动刷新。

**注意**：汇率为录入时点快照，存于 `TripCurrency.rate`，不随时间更新（符合"记录某次旅行当时花费"的定位）。

---

## 2026-06-30 · D8：设置页同行人/城市的增改删与删除保护

**背景**：设置页原本只能「添加」同行人和城市，缺编辑与删除。

**决策**：
- **补齐 CRUD**：同行人/城市均支持行内编辑（改名、换照片 / 改国家、手填坐标或勾选「重新定位」自动地理编码）与删除。
- **删除保护（软拒绝，不级联）**：删除前校验引用——同行人被任何 Trip 引用、城市被任何 Leg(出发/到达) 或 Day 引用时，**拒绝删除并提示**，而非级联清掉历史记录。理由：私人记录场景下，城市/同行人是跨旅程复用的基础数据，误删会牵连已写好的历史行程，挡住比连带删除更安全。（与「旅程删除级联清理」相反：旅程是顶层聚合根，删它就该带走其下属 Leg/Day/Entry；而城市/同行人是被复用的基础数据，反过来该保护。）
- **城市列表按国家/地区分组**：列表按国家分组展示，组标题带数量，无国家的归入「未分类」放最后；分组仅为展示，在路由内组装，不改数据模型。

**注意**：未加数据库外键级联或 `ON DELETE` 约束，引用校验在应用层（蓝图）做，保持与无 migration 策略一致。

---

## 2026-07-01 · D9：记账文件导入 & Entry 编辑/删除

**背景**：作者用记账 App（钱迹类，导出 `.xls`）记录旅行花费，希望批量导入到对应旅程，省去逐条手录。

**决策**：
- **只支持 `.xls`（老格式）**：这次的样例文件就是老格式，新增 `xlrd` 依赖解析；`.xlsx` 按需再扩展（YAGNI），避免一次引入两套解析逻辑。
- **匹配规则**：日期→找旅程内已有的天（Day 需已存在，不自动新建）；一级分类→系统分类（**为此把 Entry.category 新增「其他消费」**，共 6 类）；支出账户→币种（现金→CNY，其余需为该旅程已声明的外币，沿用录入时既有的"只能选已声明币种"约束）；备注→标题，描述留空（源数据里"商家"列全空、"备注"列全非空且是简短商家/物品名，天然适合当标题）；二级分类/商家/项目/成员列均忽略。
- **两段式入库，不新增表/字段**：日期/分类/币种三者全部匹配成功的行**立即入库**（符合"自动导入"的直觉）；任一匹配失败的整行进入待确认列表，通过隐藏表单字段回传已解析对的字段，用户只需补齐失败的几个下拉框，确认后统一入库。不引入 session 或临时表存草稿，保持无状态、无 migration。
- **不做去重检测**：同一文件重复上传会重复入库（YAGNI，私人单机场景由用户自己注意，出错可配合下面的编辑/删除撤销）。
- **顺带补齐 Entry 编辑/删除**：导入是批量操作，一旦分类/币种映射有误此前无法撤销；照搬 D8 同行人/城市的 inline-edit 模式给 Entry 加编辑（分类/标题/金额/币种/描述，不改所属天、不改图片）与删除（级联删其图片记录，不清磁盘文件，与现有做法一致，无引用保护必要，因为没有别的表引用 Entry）。

**注意**：待确认列表的编辑并未做"记住上次选择"之类的便利功能（比如自动把"其他消费"以外的陌生一级分类也记住映射）——不满足 YAGNI，真出现新的记账 App/分类再迭代。

---

## 2026-07-02 · D10：配图从 Entry 移到 Day

**背景**：原本每条消费（Entry）可挂配图（EntryImage）。实际记录时照片是「某天」的，不是某笔花费的，挂在 Entry 上颗粒度过细、录入啰嗦。

**决策**：
- **配图改挂 Day**：新增 `DayImage(day_id, path)`，`Day.images` 关系（cascade all/delete-orphan）；删除 `Entry.images`。Entry 不再有配图。
- **上传入口**：①「添加一天」表单可选传多张；②每个日卡片有「+ 添加照片」表单事后追加（路由 `POST /trips/<tid>/days/<did>/images`，校验 day 属于该 trip）。配图缩略图显示在日卡片日记下方。
- **旧表 `entry_image` 保留不清**：遵循无 migration 策略（`create_all` 只新增 `day_image`，不动旧表）。现有真实数据里 Entry 从未挂过图（导入不含图），故无数据迁移；`entry_image` 成为孤儿表，无害留存。真要清理需手动 drop（私人单机，暂不做）。

**注意**：改结构前先备份 `instance/travel.db`（`instance/travel.db.bak-*`），未清任何 Entry 数据。

---

## 2026-07-08 · D11：花费统计增强 —— 城市/累计/Top 维度 + 图表观感

**背景**：原统计页只有总花费、类别饼图、按天柱状图、按币种表，内容偏少、样式朴素。作者希望「内容全一些、效果炫一些」。

**决策**：
- **新增三个维度**（`stats.trip_stats` 追加字段，旧字段不动，保证 list/detail 页兼容）：
  - `by_city`：Day→City 聚合当天各 Entry 的人民币，无城市归「未指定」，金额降序。
  - `cumulative`：按已排序的 `by_day` 逐日累加。
  - `top_entries`：全部 Entry 折人民币降序取前 10（标题/类别/日期/金额/原币）。
- **展示**（`trips/stats.html`）：饼图→环形图（中心叠总额）；按天柱 + 累计折线合并成组合图；新增按城市横向条形图；Top 10 卡片列表。Chart.js v4 渐变填充 + 圆角 + hover/入场动画（纯观感，不改数据口径）。
- **全站容器加宽**：`.container` max-width 760→1100px（内容区太窄，留白过多）。影响所有页面。
- **XSS 防护**：城市名/条目标题是用户自由文本。城市名进内联 `<script>` 走 `_safe_json()` 转义 `< > &` 防 `</script>` 截断；Top 10 标题改在模板用 Jinja 自动转义渲染，不进 JS。沿用 `stats.py`/`stats_page` 原有的安全注释约束。

**注意**：本次按作者要求未加后端测试（`trip_stats` 新字段无单测），纯观感/图表无前端测试（项目本就无前端测试）。

## 2026-07-08 · D12：首页改为 3D 地球路线展示（第二版）

**背景**：首页 `/` 原本只是重定向到旅程列表。作者希望首页是一个可转动的地球，默认加载所有旅程，直观看到每段城市路线与所用的大型交通方式。对应设计文档 [docs/specs/2026-07-08-globe-home-design.md](docs/specs/2026-07-08-globe-home-design.md)。

**决策**：
- **渲染库选 Globe.gl（Three.js/WebGL）** 而非 D3 正射投影：最贴近「真实地球」观感，弧线/拖拽旋转开箱即用。本站为纯私人本地站，故把 `globe.gl.min.js` + `earth-blue-marble.jpg` + `night-sky.png` **下载到 `app/static/vendor/` 离线用**，不依赖联网 CDN。
- **编码：弧线颜色按「旅程」，交通方式按弧线中点 emoji**。多旅程叠加时优先让「同一趟成团」，交通方式靠 ✈️🚄🛳️… 图标区分（复用 `TRANSPORT_MODE_EMOJI`）。旅程配色按 `start_date` 升序循环取固定调色板，保证颜色稳定。
- **数据**：新增 `services/globe.py::build_globe_data()` 把所有 Trip 的 Leg 拍平成弧线 + 去重城市点，经 `{{ globe|tojson }}` 内联进 `home.html`，前端 `static/globe.js` 直接读，不另开 XHR 接口。缺经纬度或缺城市的 Leg 跳过并计数，页面底部一行小字提示。
- **联动**：右侧旅程列表 hover → 地球只亮该趟、其余变淡并旋转对准；列表标题即链接到旅程详情页（地球顺带成为入口）。顶栏品牌链接改指首页。

**注意**：按作者要求，后端 `build_globe_data` 改动很轻，**未走 TDD、无单测**；前端纯展示，靠预览人工核对。`base.html` 新增 `{% block main_class %}` 让地球页满宽脱离 `.container`。

## 2026-07-08 · D13：首页地球增加 D3 矢量渲染器 + 前端切换

**背景**：D12 用 Globe.gl 写实地球上线后，作者想要一个更轻的「矢量线框地球」观感，并能在前端切换。

**决策**：
- **两个渲染器并存，前端可切**：右上角分段开关（🌍 写实 = Globe.gl / 🗺️ 矢量 = D3 正射投影），选择记 `localStorage`，默认 Globe.gl。
- **后端零改动**：`build_globe_data()` 产出的数据与渲染库无关，两个渲染器读同一份。
- **抽统一渲染器接口**：`setFocus / focusView / initialView / resize / pause / resume`。控制器 `globe-home.js` 只管解析数据 + 列表联动 + 调度；`globe-gl.js` / `globe-d3.js` 各自实现接口。列表联动、hover 信息卡两边行为一致。
- **D3 固有取舍**：正射投影是 2D，弧线**贴球面、扁平无高度**（不像 Globe.gl 浮起），这是选它时就接受的。切到矢量模式时地球区背景转浅色（`.globe-stage.light`）贴合站点清新风。
- **离线资源**：新增 `d3.min.js`、`topojson-client.min.js`、`land-110m.json`（world-atlas 大陆地形）落地 `static/vendor/`。
- **实例缓存**：切换时缓存已建渲染器实例并 pause/resume，避免反复新建 WebGL context。

**注意**：同 D12，前端纯展示无自动化测试，靠预览人工核对（已核对：切换持久化、两模式弧线/emoji/城市点、列表联动高亮在两种渲染器下均正常）。

## 2026-07-08 · D14：每日消费拖拽排序 + 首次「保留数据」的 schema 变更

**背景**：作者要给同一天内的消费记录（Entry）加手动排序，用拖拽调整、异步保存不刷新页面。库里已有 148 条真实 Entry 数据，不能按默认「删库重建」处理。

**决策**：
- **Entry 新增 `sort_order` 整数列**，relationship 排序改为 `sort_order, created_at`（`created_at` 降为次级）。新增记录排到当天末尾（`max(sort_order)+1`）。
- **首次做「保留数据」的 schema 变更**：偏离 D6 默认的「改模型删 `instance/travel.db` 重建」。因为已有真实旅行数据，改为**先备份（`travel.db.bak-*`）→ 手动 `ALTER TABLE entry ADD COLUMN sort_order` → 用窗口函数按 `created_at` 回填每天现有顺序**。仍不引入 Alembic（一次性手工够用）。
- **异步 reorder 接口**：`POST /trips/<id>/days/<day_id>/entries/reorder`，请求体 `{"order": [entry_id,...]}`，校验提交的 id 恰好等于该天全部 Entry（防漏项/串天），按下标写 `sort_order`，返回 JSON。前端 `static/entry-reorder.js` 用原生 HTML5 拖放（不引第三方库，守「极少 JS」约定），拖完 `fetch` 提交、失败弹提示。

**踩坑**：`blueprints/trips.py` 里有个视图函数叫 `list`（`bp.route("/")`），它**遮蔽了内建 `list`**。函数体内 `list(...)` / `isinstance(x, list)` 会拿到这个视图函数而报错——「清空消费」和本次 reorder 都栽过一次。约定：**本文件内不要用内建 `list`**，改用切片 `seq[:]` 或 `[int(x) for x in ...]` 等写法。

**注意**：前端纯展示、后端改动轻，按作者要求**未走 TDD、无单测**；后端已用 test_client 做端到端核对（正常反转/缺项 400/坏载荷 400/错 trip 404/可恢复），前端核对渲染标记（draggable/data-entry-id/脚本引入）齐全。

## 2026-07-08 · D15：首页地球增加卫星模式（深缩放到街道级）

**背景**：写实模式是单张 4K 贴图，放大就糊（无 LOD）。作者要能放大看真实细节，且明确"虽私人但可联网"。见 [docs/specs/2026-07-08-globe-home-design.md](docs/specs/2026-07-08-globe-home-design.md)。

**决策**：
- **加第三个模式 🛰️ 卫星**（与写实/矢量并列，记 `localStorage`），仍用 Globe.gl 渲染器（`globe-gl.js` 的一个变体），只把球面从贴图换成 **Esri World Imagery 卫星瓦片**（免 token、免费、需署名，联网即用）。是唯一联网模式。
- **弃用 Globe.gl 内置 `globeTileEngineUrl`**：实测其瓦片层级被 `thresholds` 卡在 z≈4、源码 `if(level>7)` 硬顶 z=7，只到区域尺度。改**自绘 LOD 瓦片图层**：按相机高度 `pointOfView().altitude` 选 Web Mercator 缩放级、只加载视野附近瓦片（经向环绕/纬向截断）、监听 controls `change`（rAF 节流）按级重算 → 可放大到 z≈14 街道级。
- **three 实例复用**：自绘瓦片要用 globe.gl 内部那份 three 建材质（否则多实例 `instanceof` 冲突）。globe.gl 2.46 是 `window.THREE ? window.THREE : 内置` 的 peer 模式，但 three ≥0.179 只发 ESM。故 `home.html` 用 `import * as THREE` 设 `window.THREE = Object.assign({}, THREE)`（可写副本，避免冻结命名空间被写报错），再按序动态 `load()` globe.gl 等脚本。
- **瓦片渲染三连坑**：① 瓦片球面片正面朝内 → 材质 `DoubleSide`；② 瓦片与球体同半径被底图 z-fight 盖住 → `tileAltitude(0.02)` 抬成球外壳；③ 底图不透明写深度遮住背面瓦片、兜住间隙 → 形成实心球。

**注意**：纯前端改动，无后端、无测试；三种模式（写实/卫星/矢量）均在预览中人工核对：渲染、深缩放到 z14、切换与联动、外部 three 下写实/矢量不回归。**本条随「清空消费」批次一起提交**（追加时 DECISIONS.md 正被那批占用）。

## 2026-07-09 · D16：矢量地图放大看细节（两级懒加载详情层）

**背景**：矢量模式（globe-d3）只有一份 `land-110m` 粗轮廓，放大无细节。作者要"放大看省/州界"，并追加河流+湖泊。见 [docs/specs/2026-07-09-globe-vector-detail-design.md](docs/specs/2026-07-09-globe-vector-detail-design.md)。

**决策**：
- **两级图层 + 阈值触发**：`zoom < DETAIL_ZOOM(2.5)` 走现有 `land-110m`；越过阈值**懒加载**四份 10m 数据（首次跨阈值才 fetch，不压首屏）并绘制详情层。仅 D3 渲染器消费（写实/卫星靠贴图/瓦片本就有细节）。
- **admin-1 用 `topojson.mesh(...,(a,b)=>a!==b)` 一层出国界+省界**：只取相邻共享边，省一个单独的国家边界文件；外圈海岸线由陆地填充体现（故 mesh 过滤掉非共享边）。是**单个 SVG `<path>`**，不是数千多边形。
- **10m 是 D3 正射矢量的实用上限**：更细（区县/街道）得换矢量瓦片+墨卡托滑动地图，破坏 3D 地球形态；街道级清晰交给**卫星模式**（栅格瓦片）。矢量球"更清晰"= 在 10m 内加更多**种类**（河/湖），而非更高分辨率。

**踩坑（性能）**：详情态逐帧重投影约 300ms/帧（约 3fps）。profiling 定位到 **`land-10m` 单层就 187ms**，且**调 `projection.precision` 几乎无效**（顶点数瓶颈，非自适应采样）。两处修正：
- **海岸线填充改用 `land-50m`**（非 10m）——比 110m 精细得多，重投影成本低一个量级；admin/河/湖 mapshaper 加大简化。懒加载体积 ~7MB→~3MB。
- **交互降级（interaction-LOD）**：`draw(fast)` 加参数；拖拽/缩放/补间中只画 `land-110m` 粗轮廓（约 7ms/帧），交互停止（drag end / 滚轮停手 180ms 去抖 / 补间结束 / 懒加载 resolve）再补一帧完整详情。静态看永远是详情，只有正在动时临时降级。

**注意**：纯前端改动，无后端、无测试；预览中人工核对——放大出现省界/河/湖、缩小消失、四文件各只拉一次、交互流畅、写实/卫星模式不回归。

## 2026-07-09 · D17：矢量详情层改用视口裁剪（替代 D16 的交互降级）

**背景**：D16 的交互降级上线后，作者反馈两个问题——① `ZOOM_MAX=8` 太小，无法持续放大；② 交互中只画粗轮廓，导致缩放时"看不到变化"、拖拽时详情/海岸线闪进闪出，观感差。根因：交互降级为了性能牺牲了"详情全程可见"，而作者要的正是全程可见。

**决策**（推翻 D16 的 interaction-LOD，改为视口裁剪）：
- **`ZOOM_MAX` 8 → 50**：可持续放大到省/市级。
- **视口裁剪替代交互降级**：详情几何全部拆成**要素数组**（land→按大陆/岛屿拆多边形、admin 边界 mesh→拆线段、河/湖本就是要素集），每要素预缓存球面外接（中心 `_c`+角半径 `_r`，`d3.geoBounds` 一次算）。每帧只画「外接与视口相交」的要素：`keep(f)=geoDistance(中心,f._c)<cap+f._r`，`cap=asin(min(1,半对角/scale))`。**详情全程可见、不再降级**；深放大可见要素骤减 → 重绘 300ms→**15ms（约 60fps）**，越放大越流畅越清晰。
- **权衡**：刚过 `DETAIL_ZOOM`（视口约含整块大陆）时多数要素仍可见、裁剪帮不上忙，该带**主动拖拽** ~80ms/帧（约 12fps）；静态观看始终瞬时。作者已确认接受此权衡。

**踩坑**：world-atlas `land-*.json` 的 `objects.land` 是 **GeometryCollection**，`topojson.feature(...)` 返回 **FeatureCollection**（无 `.geometry`）；其内是**单个 MultiPolygon**，需取 `.features[0].geometry.coordinates` 再按多边形拆分，否则 `.geometry.coordinates` 报错（且在 Promise 里被吞、无控制台报错，表现为详情静默不出）。

## 2026-07-13 · D18：上传图片按归属分目录（替代 uploads/ 单层平铺）

**背景**：所有上传的图片（旅程配图 + 同行人照片）都用随机文件名平铺在 `uploads/` 根目录下，传多了很乱，也难按旅程归档。检查现状时发现更严重的：`uploads/` 里 50 个文件只有 3 个被 DB 引用，其余 47 个是孤儿——大量来自测试污染（见下"踩坑"）。

**决策**：
- **按归属分子目录**：旅程配图落 `uploads/trips/{trip_id}/`，同行人照片落 `uploads/people/`。`save_upload(file, folder, subdir=...)` 新增 `subdir` 参数（仅服务端内部拼接，不接受用户传值），库里存的相对路径含子目录（如 `uploads/trips/4/xxx.jpg`）。
- **`delete_upload` 保留子路径**：原实现只取 `basename`（历史上是防穿越的粗暴手段），改为剥掉 `uploads/` 前缀后保留子目录，再用 `os.path.commonpath` 规范化校验必须落在 `upload_folder` 内（防路径穿越）。兼容旧的平铺路径。
- **服务路由无需改**：`/uploads/<path:filename>` 本就吃子路径，`send_from_directory` 自带越界保护。
- **一次性清理**：备份 DB 后，删除全部 `DayImage` 行 + 清空 `uploads/`（含 47 个孤儿），作者后续重新上传。

**踩坑（测试隔离）**：`conftest.py` 的 `app` fixture 只覆盖了 DB URI，**没覆盖 `UPLOAD_FOLDER`**，导致上传相关的蓝图测试把图片写进**真实** `uploads/` 目录——这正是那 47 个孤儿文件的主要来源。修复：fixture 用 pytest `tmp_path` 把 `UPLOAD_FOLDER` 指到临时目录。修复后全量测试跑完 `uploads/` 保持为空。

## 2026-07-14 · D19：引入 ROADMAP.md 里程碑文档，规划第三版「回顾·统计·分享」

**背景**：前两版的版本概念散在 CLAUDE.md「版本」节和设计文档「版本划分」节，各自一行；实现计划是一次性的（D6：做完归档不维护）。第三版是一篮子相互独立的功能（作者定的方向：回顾与重温、统计与洞察、展示与分享——录入侧已够用），且明确「一个版本没做完的项可以随时捡起来继续」——现有四类文档没有一类装得下这种**可长期维护的功能清单**。

**决策**：
- **新增根目录 `ROADMAP.md` 作为第五类文档（活清单）**：按大版本分节，每个功能一个勾选项，上线即勾选；未完成项跨迭代保留，新想法进「候选池」。更新时机：版本规划变化、功能上线勾选。它只管「做什么、做没做」，**不管怎么做**——每个功能动手前照旧出各自的 spec 与 plan。
- **第三版范围**：核心枢纽是**旅程故事页**（按天沉浸式回放，一套模板同时作为回顾浏览/全屏演示/静态导出的底座，投入产出比最高故最先做）；另有那年今日、全文搜索（FTS5）、人生足迹总览、年度报告、全屏演示模式、静态 HTML 导出、局域网只读+移动端。完整清单与建议顺序见 [ROADMAP.md](ROADMAP.md)。
- **修订第一版 YAGNI 两条**：「跨旅程全局统计」实际已被跨旅程消费看板部分实现，第三版继续扩展（人生足迹/年度报告），从「明确不做」移除；「对外分享」收窄为**不做公网发布/评论/账号**，本地形态的分享（静态导出、局域网只读、当面演示）进入第三版范围。设计文档第 7 节已同步。

## 2026-07-14 · D20：照片拖拽上传 + HEIC 自动转 JPEG

**背景**：作者照片都在 Mac「照片」App 里，`<input type=file>` 无法从密封包 `photoslibrary` 按张选图（已确认走不通）；可行路子是把「照片」App 里的图直接**拖到网页**，系统会给出真实文件，但常是 HEIC——Chrome 不认 HEIC，服务端也只收 png/jpg/jpeg/gif/webp。

**决策**：
- **`save_upload` 内建 HEIC/HEIF 转码**：新依赖 `Pillow` + `pillow-heif`（`pillow_heif.register_heif_opener()` 注册进 Pillow 后 `Image.open` 直接能读 HEIC）。HEIC/HEIF 走 `ImageOps.exif_transpose` 按 EXIF 转正后存为 JPEG（`.jpg`），非 HEIC 原样保存不重新编码。
- **拖拽区做成异步局部上传**：`add_day_images` 路由按 `X-Requested-With: XMLHttpRequest` 区分——异步请求返回 JSON（新图 id/url/delete_url），前端 [photo-drop.js](app/static/photo-drop.js) 就地插入 `.day-photos`，不刷新页面；普通表单 POST 行为不变（重定向 + flash），作为拖拽失败/不支持时的兜底。
- **前端不引入第三方库**：原生 `dragenter/dragover/drop` + `fetch`，与已有的 `entry-reorder.js` 拖拽排序风格保持一致。

**已知缺陷（未修，留待后续）**：真机导出的 HEIC 若带某些 EXIF 方向标签，`ImageOps.exif_transpose` 在读 `getexif()` 时会抛 `SyntaxError: not a TIFF file`，导致该请求 500。诡异之处：同样字节内容，走 pytest 的 `FileStorage(BytesIO(...))` 或独立脚本用 `SpooledTemporaryFile` 复现均正常，只有真实浏览器 fetch 的 multipart 上传会触发——尚未定位到 werkzeug 解析出的文件流与 pillow-heif 写回 EXIF 段之间具体的差异根因。已验证：常规 JPEG（非 HEIC）拖拽全程正常；本次先合并，此 bug 留到下次专门排查修复。

## 2026-07-17 · D21：跨旅程看板搬去足迹页；跨年旅程整趟归开始年

**背景**：第三版「统计与洞察」要做人生足迹总览页与年度报告。动手前发现**旅程列表页早已是个跨旅程消费看板**（总花费/旅程数/里程/国家/城市 Top5/最贵最便宜/消费构成/各旅程对比），与「人生足迹总览页」的清单大面积重叠——ROADMAP 那条里真正缺的只有在途天数、里程碑、逐年对比三块。

**决策**：
- **看板整体搬到 `/insights/`，旅程列表页回归纯列表**。理由是避免同一份 KPI 在两处维护；总览页成为唯一的「统计首页」。列表页只留标题 + 创建入口 + 旅程卡片。
- **两页共享 `services/lifetime.py`**：口径（年份归属、在途天数、新解锁国家）只在这一个模块定义一次，金额换算与里程分别复用既有的 `services/stats.py` 与 `services/distance.py`，不重写。
- **跨年旅程整趟归 `start_date` 的年份，不按天拆**。库里仅 1 趟受影响（`201612 - 台北`，2016-12-29 → 2017-01-02，跨 2 天）。按天拆虽更精确，但会让同一趟旅程同时出现在两年的报告里（「2017 年你走了 N 趟」里混进一趟 2016 年出发的），叙事别扭，且年度报告的旅程数与总览页对不上。为 1 趟旅程的 2 天不值得引入这个复杂度。
- **逐年对比空档年补零**：2020、2022 无旅程也画零柱——停摆本身是信息，补零才看得出疫情三年的形状。
- **年度报告地图复用故事页的 `STORY_MAP.render()`，但加 `opts.frameAll` 跳过取景裁剪**。那套裁剪（甩开出发地与远程中转，见 D18 时期的 story-map）是为单趟旅程调的，一年多趟跨洲时会把画框框偏；年度报告要的是「那年走遍了哪」的全貌。这是该渲染器的第三个消费方（故事页、放映壳、年度报告），opts 开关比分叉出第二个渲染器划算。

## 2026-07-22 · D22：分类/标签统计改成三个同构的两栏 Miller 榜

**背景**：月度/年度/整体三页原本只有扁平的「一级分类排行」+ 另一张全局「二级分类 Top 10」+ 「标签榜」卡片。想看某分类下具体哪几笔大额消费，得跳去流水页反复筛。中途先做过「一级→二级→单笔」的三栏 Miller 下钻（试过上下手风琴被否，作者要向右展开、左侧固定），最后作者定稿为**三个平行的两栏榜**：一级分类榜、二级分类榜、标签榜。

**决策**：
- **三个榜同构，共用一套渲染**：都是两栏 Miller 列——左栏排行（固定常驻，金额降序）、点某项右栏出它名下单笔支出 Top N。前端 [expenses-stats.js](app/static/expenses-stats.js) 抽出 `expBoard(mount, data, fmt)` + `expMillerItem` / `expMillerRecords`，三个榜只有左栏一行的标题/副文不同，用 `expFmtCat1/Cat2/Tag` 注入。服务端对应 `category_level1_board` / `category_level2_board` / `tag_drilldown`，各算好整棵内联为 JSON（`_board_json` 一个序列化器通吃）。**不做逐级/逐榜 AJAX**——纯本地单机、数据量有界，一次算好交互零延迟。
- **`record_limit=15` 给右栏封顶**：payload = 榜项数 × 15，即便 14k 条流水也只内联几百条记录。看更多单笔仍去流水页筛。
- **二级分类榜跨一级全局排序**（不按一级分组），直接挂一级、无二级的记录跳过——与旧「二级分类 Top 10」口径一致，只是从 Top 10 放开成全部 + 可下钻单笔。故 `_rank_subcategories`、`category_drilldown`（三栏下钻）、`_rank_tags`（只回 Top 10）一并退役。
- **一级分类榜保留一级占比环图**（左栏顶部居中）；一级名下单笔跨其所有二级聚合。
- **显示顺序定为 一级榜 → 二级榜 → 标签榜**（作者指定）；总览页的「逐年明细表」挪到三个榜之后、页尾。三个榜都吃满整卡宽度，原来塞半宽两列的 `overview-2col` 随之退役。
- **每行标题独占一行、金额落副行右侧**：窄栏里七位数金额否则会把标题挤成「居…」。

## 2026-07-27 · D23：二级分类只管渠道，品类归标签

**背景**：日常消费跑满十年（14938 条，2017-01 起）后回看分类体系，发现 seed 里从 myMoney 抄来的一批二级分类**一条记录都没有**——买买买买下的 日用品 / 衣服鞋包 / 电子产品 / 玩具、食品酒水下的 烟酒茶、行车交通下的 维修保养。原因是实际录入时这些内容全走了同名**标签**（衣服鞋包标签 345 条、烟酒茶 273 条…），二级分类那一格填的是「网购 / 商场 / 超市市场」这类渠道。等于同一个「买什么」维度在分类和标签里各设了一份，分类那份没人用，只在录入下拉框里添乱。

**决策**：
- **确立分工**：一级=领域，二级=**渠道/场景**（超市市场 / 网购 / 商场、早餐 / 午餐 / 晚餐），标签=**具体是什么**（菜系、品类、停车缴费/充电这类细目）。两边不再设同名维度。
- **删掉上述 6 个零使用二级分类**，并按此口径合并三个语义重复的：`代购`→`网购`（12 条）、`座机费`→`手机费`（1 条）、`送礼`→`网购`（1 条，作者指定）。删掉遗留的「测试标签」。改完全库无零使用的二级分类、无零使用标签。
- **标签与分类字面重复不算问题，只要有增量**：「超市」标签 1845 条里 1838 条挂在「超市市场」分类下，几乎完全重复，但作者选择保留——标签是从记账 App 十年连续带过来的原始信息，删了不可逆，且不影响统计（二级榜与标签榜本就是两个平行维度）。**只删没人用的，不删只是冗余的。**
- **`CATEGORY_ICONS` 不跟着删**：它是「按名字猜图标」的查找表，条目比 seed 多是正常的——导入 .xls 时源头若还有这些分类名会自动补建，那时仍希望配上图标。

**未处理，留作已知问题**：导入服务 `_get_or_create_category` 照抄 .xls 里的分类名，所以下次从 myMoney 导出若仍带「代购/座机费/送礼」，这三个分类会被重新建出来——没做导入侧的改名映射表，等真的再导一次数据时再说。另：「行权」（11 条 ¥52.5 万）性质是投资而非消费，会把按月/按分类的支出统计拉出尖峰，统计口径是否排除尚未决定。

## 2026-07-27 · D24：清掉支出侧最后一批长尾二级分类

**背景**：D23 后重新审视一二级分类与标签的合理性，发现支出侧还剩 4 个二级分类年份很老、量极小、且与同一级下的另一个分类语义重叠——休闲娱乐/腐败聚会（6 条，止于 2023-07，与「休闲玩乐」同义）、医疗保健/美容费（2 条，止于 2020-08，与「保健费」同义）、金融保险/银行手续（1 条，2017-02）、其他杂项/充钱储值（1 条，2017-02，与「其他支出」同义）。

**决策**：四个并入同级更常用的分类——腐败聚会→休闲玩乐、美容费→保健费、银行手续→其他支出、充钱储值→其他支出，共 10 条记录改 `category_id` 后删源分类。四个名字本就不在 `DEFAULT_CATEGORIES` seed 里（只在 `CATEGORY_ICONS` 里挂着猜图标用），代码不用改。改完支出侧一二级分类已无任何长尾（每个二级分类都在 2024 年后至少用过一次，或是仍在承载完整历史事件如「人情往来」）。

同批评估里识别出两个真实缺口，**暂缓新增，留待观察**：「娃」相关开销散在 休闲玩乐/玩具/培训进修 之间无法合并统计；Claude/Apple 订阅类支出目前落在「休闲玩乐」下没有区分。建议后续加标签「娃」「订阅」，但这次先只做删除/合并，不新增维度。

## 2026-07-27 · D25：医疗保健改按就医渠道三分——公立 / 私立 / 药品

**背景**：原有的 治疗费 / 药品费 / 保健费 是按「花在什么上」分的，与 D23 定的「二级=渠道/场景」口径不符，且实际数据是乱的——治疗费 94 条里最大的一批其实是私立（美中宜和生产 ¥6.3 万、嬉月阁·禧月阁月子中心 ¥10 万+、卓正齿科系列），保健费 15 条里混着爱康体检、卓正会员，也混着清华池搓澡和蛋白粉。作者提出改按公立/私立医院分，评估后落成三分。

**决策**：
- **三分而不是二分**：`公立医疗` / `私立医疗` / `药品费`。药品费单列是因为药店买药与补剂（钙、益生菌、蛋白粉、海盐水）既不属公立也不属私立，是第三种渠道，并进任一侧都会把它抹掉。
- **命名不用「医院」用「医疗」**：卓正是诊所、爱康是体检机构、月子中心不是医院，「医疗」都罩得住；也不用「常规医院」——「常规」的反义是「特殊」而不是「私立」。
- **实现是改名 + 逐条搬迁，不是纯改名**：`治疗费`→`公立医疗`、`保健费`→`私立医疗`（复用原分类 id 保留历史），再按两条规则搬 109 条记录：①备注命名了机构的按机构归（卓正/美中/嬉月阁/禧月阁/爱康 → 私立；儿研所/协和/友谊/清河/积水潭/北医三院/东直门/海淀/口腔医院/999/120 → 公立）；②**怀孕标签期（2018-05 ~ 2020-08）的就医全部归私立**——那一程产检、生产、月子中心全在美中宜和体系内。搬完 公立 69 条 ¥1.9 万、私立 52 条 ¥26.9 万、药品 41 条 ¥1.8 万。
- **顺带把 4 条非医疗记录移出医疗保健**：清华池搓澡/修脚 ×3、云何梵按摩 ×1 → 休闲娱乐/休闲玩乐。（月子期的理疗/按摩 2 条留在私立医疗，属月子中心服务。）
- **`CATEGORY_ICONS` 保留 `治疗费`/`保健费` 两个旧名**：myMoney 那边分类没改，下次导入 .xls 还会带这两个名字自动补建，那时仍希望有图标。图标沿用 公立 🏥 / 私立 🩺 / 药品 💊。

**判断存疑、按默认归了私立的 13 条**（备注没写机构，按价位与服务类型推断，作者可随时翻案）：2021 年牙周刮治四次（¥764~879/次）、补牙涂氟 ¥1010、涂氟 ¥488、补牙 ¥560、眼科 6 次 ¥1789——涂氟/窝沟封闭/视力套餐是卓正的招牌项目，价位也高于公立；另有 2018 孕前检 ¥1239、颐和问诊 ¥88，以及怀孕期两条备注写「药品」的（¥69.6/¥65.8，随产检发生故归私立而非药品费）。反过来 3 条只写「疫苗」没写机构的（¥798、¥339、巴斯德三价 ¥270）留在了药品费。

## 2026-07-27 · D26：休闲娱乐新增「个人护理」，收拢理发/搓澡/按摩

**背景**：D25 医疗保健三分后，理发（15 条 ¥4399，一直挂在居家物业/维修保养下）和清华池搓澡按摩（4 条 ¥746，D25 里临时放进休闲玩乐）都没有合适的家。曾考虑塞进医疗保健，但作者指出两个问题：一是会和洗牙这类真正的口腔医疗产生「不知道选哪个」的模糊地带；二是理发这件事第一反应根本不会往医疗保健想，录入时找不到。

**决策**：
- **新增二级分类「休闲娱乐 / 个人护理」**（不放医疗保健下）。判断标准一句话：**找医生→医疗保健，找师傅/技师→个人护理**，跟洗牙的边界从「同一个一级下的模糊二选一」变成「跨一级的清晰分界线」，不会混淆。
- 参照系是同一个一级下已有的「运动健身」——健身卡、游泳课这类「花在自己身体上但不是看病」的支出，一直安稳待在休闲娱乐里，个人护理是它的同类项，位置顺理成章。
- 收拢 19 条 ¥5145：理发/剪头 15 条（从居家物业/维修保养搬来，含 3 条只在备注写了没打标签的，顺带补上「理发」标签，现在个人护理下 15 条全部有标签覆盖）+ 清华池搓澡修脚 3 条、云何梵按摩 1 条（从 D25 临时安置的休闲玩乐搬来）。
- `DEFAULT_CATEGORIES`、`CATEGORY_ICONS`（图标 💇）、spec 3.5 节同步。

## 2026-07-27 · D27：走势页标签按「标签组」二级分组；`expense_tag` 加列 `group_name`

**背景**：标签模式原先平铺 53 个标签在一起，其中「食品酒水」下就占了 30+ 个（各种菜系 + 火锅 + 甜品饮品 + 便餐小食），淹没在一堆里不好找。按现有分类树分不出这层——分类树的二级是「早/中/晚餐」这种**餐次**，跟菜系/火锅是正交的两个维度，数据里根本没有这层信息。

**决策**：
- **`ExpenseTag` 加可空列 `group_name`**（标签组：中餐菜系/火锅/异国料理/甜品饮品/便餐小食/酒水，`TAG_GROUPS` 定死这 6 个），走势页左栏据此在一级分类下再缩进一层；`group_name` 只影响这一处展示，不参与任何统计口径。列名不用 `group`——SQL 保留字。
- **有真实数据，不能走"删库重建"的默认无 migration 策略**（D6）：改前手动备份 `instance/travel.db` 到 `instance/backups/`；`app/__init__.py` 新增 `_ensure_added_columns()`，在 `create_all()` 之后对已存在的表做 `ALTER TABLE ADD COLUMN`（按列名是否已存在幂等跳过），新库因为 `create_all` 已经建好这列则直接跳过。以后再有类似"老库加列"的情况，照这个口子加。
- **首次填充用启发式规则 `guess_tag_group`**（名字精确命中 + 「XX火锅」「XX菜」后缀兜底），`seed_tag_groups()` 只在"库里所有标签都还没设过分组"时跑一次，跑完即以手工结果为准，不会覆盖。分类管理页给每个标签加了下拉可改。跑下来 53 个标签自动分对 34 个（食品酒水下的吃喝类），其余 19 个（衣服鞋包、购车保养这类跟餐饮无关的）留空，未分组的标签排在该一级分类最后、不缩进。
- 启发式有猜错的（如「烟酒茶」实际主要是买烟酒茶叶这类商品，不是「酒水」场景消费），手工改回未分组即可，不追求猜测规则 100% 准。

## 2026-07-27 · D28：固定收支不做定时任务，改「水位线补记」；新表 `expense_rule`

**背景**：翻账时发现房租收入、房租支出、公积金都有整段整段的空洞——房租收入停在 2025-04 而其他流水记到 2026-07，中间 15 个月全丢；房租支出月度那条同一天断的；公积金停在 2023-11 之后 32 个月没有。这些都是「金额固定、周期固定」的收支，全靠手工记，一旦某个月忘了就再也想不起来补，越积越多。

**决策**：

- **不引入 APScheduler 之类的常驻调度**。这是本地手动启动的应用（`python run.py`），关机期间定时任务照样不跑，漏记的病根一点没治。改成**水位线补记**：规则记着「从哪个月开始、每几个月一笔」，进日常消费模块时把从起始月到本月之间还缺的记录一次性补齐。关机三个月再打开，三笔一起补上，结果与天天开着完全一样——这才是没有常驻进程的应用该用的模型。
- **周期只做「每 N 个月 + 固定落 1 号」**，不做按周、按任意日期。落在哪几个月由 `start_month` 定相位（起始 2 月 + 间隔 3 → 2/5/8/11 月），所以不用单独存「哪几个月」。
- **一条规则 = 一个金额 + 一段时间**（`start_month` / 可空 `end_month`），不在单条规则里塞多段金额区间。房租从 4800 涨到 4900 再到 5000，就是三条首尾相接的规则；查询、展示、改动都简单，代价只是规则表多几行。顺带白送了回补历史的能力：起始月填 2020-03 就把五年补齐。
- **规则只认自己名下的记录**（靠 `expense_record.rule_id` 回指），不去猜「这个月是不是已经手工记过一笔房租」。判断「是不是同一笔」很主观，误判会造成静默漏记——正是这功能要治的病；相比之下多出来的记录在流水里一眼可见、随手可删。代价是起始月填错会一次多出几十条，所以表单加了实时 `preview`：保存前先说清楚将补记多少条、覆盖哪段、合计多少。
- **自动写库必须有干净的后悔路**：删规则默认把已生成记录转为手工记录保留（`rule_id` 置空、`source` 改 `manual`），带 `purge=1` 才连记录一起删。这也是选「加 `rule_id` 列」而不是「复用 `fingerprint` 字符串去重」的主要理由——后者想撤销只能 LIKE 匹配。
- **幂等两层保底**：`materialize` 只插「该记的日期」减「已有的日期」的差集；数据库层再加唯一约束 `(rule_id, date)`。SQLite 里 `NULL != NULL`，所以手工/导入记录（`rule_id` 为空）互不影响。
- **改金额只影响以后**，已生成的记录保留当时的数——那才是当时真实发生的。
- **`_ADDED_INDEXES` 补上 D27 那个口子的另一半**：`create_all` 既不给已有表补列、也不补索引。D27 加了 `_ADDED_COLUMNS` 解决前者，但唯一约束在老库上照样不存在，「幂等由数据库兜底」只对新库成立。照同样的套路加 `_ADDED_INDEXES`（按索引名是否已存在幂等跳过），两张清单一起在 `create_app` 里跑。

## 2026-07-27 · D29：默认入口改为日常消费，地球首页挪到 `/travel`

**背景**：日常消费模块用得比旅行记录频繁得多，但 `/` 一直是地球首页，每次打开都要多点一次顶栏切换。

**决策**：`main.py` 的根路由 `/` 改成重定向到 `/expenses/`；原来挂在 `/` 上的地球首页视图函数改名 `index`→挂到 `/travel`（endpoint 名不变，仍是 `main.index`，`base.html` 里 `url_for("main.index")` 不用改）。顶栏「✈ 旅行记录」入口照常可用，只是地址从 `/` 变成 `/travel`。

## 2026-07-28 · D30：「记一笔」改为流水页原地录入，保存走异步 + 「再记一笔」

**背景**：录一笔要跳到 `/expenses/new` 整页表单，存完跳回列表——补录时往往一次要记好几笔（一天的餐费、当月漏掉的几笔），来回跳页把节奏全打断了。行内编辑早就是「点一行原地下拉成表单」，新增却还停在老路子上，两种操作手感割裂。

**决策**：

- **新增表单直接长在流水页上**：顶部「+ 记一笔」原地展开一行录入区，控件复用行内编辑那套 `exp-inline-*`（收支/日期/金额/一级/二级/标签/备注挤一行，窄屏自然折行），新增和编辑从此是同一副长相。
- **表单挂在 `#exp-results` 外面**。列表刷新是整块 `innerHTML` 替换，表单放里面会被连同结果一起冲掉——正在填的内容就没了。
- **「再记一笔」= 保存当前这条 + 表单原样留着**。补录相邻的几笔，往往只有金额和备注不同，日期/分类/标签都一样；保存后不清空、把焦点送回金额并全选，改两个字段就能接着存。清空重填反而是倒退。
- **保存后按「当前筛选」重刷列表，而不是把新行插进 DOM**。列表有月份分组、当月小计、按月/按日两种布局，手工插一行要同步维护这些，容易和服务端算的数对不上；重刷一次由服务端统一算，永远一致。为此 `applyFilters()` 改成返回 promise。
- **新记录可能压根不在当前筛选范围内**（筛着 2025 年却记了今天的账），这时"保存成功"和"列表里没看到"会同时出现，像是没存上。所以刷完列表用返回的 `id` 找一遍——**折叠着的月份还在 `<template>` 里没进 DOM，也要一起找**，否则只是被折叠了却报"看不到"——真找不到才提示"已保存，但不在当前筛选范围内"。给 `exp_item` 宏加 `data-record-id` 就是为这个。
- **`/expenses/new` 整页表单保留**，不删。它是 JS 出问题时的兜底入口，也是 `create()` 非 XHR 分支的天然去处，留着几乎零成本。

## 2026-07-28 · D31：指纹口径收成一份，重刷只修真正错的

**背景**：批量改分类、手工新增之后再点「刷指纹」，总是报"有更新"——说明写入时算的指纹和重刷时算的对不上。查下来是**金额归一化各写各的**：导入拿到的是 xls 里的 float（`Decimal(str(18.1))` → `'18.1'`），手工表单拿到的是字符串（`Decimal('18.50')` → `'18.50'`），从库里读出来的是 `Numeric(12,2)`（`Decimal('35.00')` → `'35.00'`），同一笔钱三种写法、三个指纹。真实库里有 5 条这样的记录（都是手工编辑过的），它们的指纹再导同一份账单时认不出来，会重复插入——正是去重要防的事。

**决策**：

- **自然键的归一化只留一份实现**：`natural_amount` / `natural_key` / `record_natural_key` / `fingerprint_of` 全部收进 `services/expense_import.py`，导入解析、手工增改、批量改、重刷四个入口都从这里拿 key。蓝图里原来自己拼 `cat1|cat2`、自己传 `record.amount` 的那套（`_category_pair` + `compute_fingerprint`）删掉。**指纹是跨入口的一致性契约，不能让每个调用方各自拼串**。
- **金额归一：先定死两位（库里就是这个精度），再走一趟 float 去尾随 0**。第二步纯粹是为了兼容历史指纹——只按两位量化会让 `'18.1'` 变成 `'18.10'`，老库 15025 条指纹全部失效。加上这一步，真实库跑下来只有那 5 条错的会变，其余分毫不动；顺带把「xls 里给了 3 位小数、入库被舍成 2 位」这种老口径对不上的情况也一并修了。
- **重刷改成「只修真正错的」**：同一自然键的 N 条记录，指纹只要占满 seq 0..N-1 这个集合就是对的，**谁占哪个不重要**。所以先留下指纹已经落在本组集合里的，剩下的补空位——而不是按 id 顺序一律重排。这样重刷幂等（连刷两次第二次必然 0 变更），"有更新"这个数字才真正代表"有东西错了"。
- **删重复记录留下的 seq 空洞由重刷补上**：两条同款只剩一条、却占着 seq=1 时，再导这份账单算的是 seq=0，认不出来又插一条。重刷会把它落回 seq=0。
- **改指纹分两趟走：先全部清成 NULL，flush，再写新值**。组内两条互换指纹时中间态会撞 UNIQUE 约束；SQLite 里多个 NULL 可以并存，清空是安全的中转。
- **不变量由「写入」四个入口共同维持，重刷只是修理工具**：新增/编辑/批量改走 `next_free_fingerprint` 补第一个空位，**删除走 `compact_fingerprints_around` 把同组往前挪补上空洞**——原来删完只能靠人记得去点「刷指纹」，不点就一直漏着。
- **seq 的分配策略必须是三套，不能强行合并**：导入解析只能按「文件内第几条」数（它必须**不查库**——查了的话第二次导同一份文件会算出别的 seq，去重全盘失效），单条增改只能按「库里第一个空位」（不可能为一条记录重排全库），全量重刷才能按组整体规划。三者不共用代码，但共用同一个**不变量**和同一套 key 计算，这才是"统一"该统一的层次。

## 2026-07-29 · D32：三餐标签定一条判据——能判菜系归菜系，判不出才归业态

**背景**：三餐 3758 条里，「主食」这个标签同时装着 25 元一碗刀削面和 218 元的老边饺子，三档金额几乎均匀分布（<40 元 34 条 / 40-80 元 29 条 / ≥80 元 45 条）；「快餐」1077 条内部其实是四种东西（便利店均价 9 元、连锁快餐 36 元、中式快食店 36 元、空备注 13 元）；「烧烤」均价 239 元、最高 1030 元，却挂在「便餐小食」组下。根子是**同一个字段里混着两套判据**——一半按"吃的什么菜"打（菜系），一半按"什么形态的店"打（业态），边界上就永远吵不完。

**决策**：

- **判据只留一条：能判菜系的归菜系，判不出的才归业态兜底**。业态标签是**兜底**，不是与菜系平行的维度。判据**不看金额、不看餐次、不看是不是"随便对付"**——所以 25 元的胡辣汤和 218 元的老边饺子在菜系榜上并列是预期结果，不是 bug。
  - 曾考虑过反过来（先分"快食 vs 正餐"再分菜系），因为现有数据自发形成了 7 倍价差的分野（便餐小食中位数 24 元 vs 中餐菜系 178 元），改动量也最小。**否掉的理由**：那条分野保住的是"这顿怎么吃的"，而记账最终想回答的是"这些年吃了什么菜"；把菜系信息散在业态桶里，菜系统计就永远是残的。
  - 也考虑过业态与菜系拆成两个字段各打各的（最准），**否掉的理由**：`expense_record.tag_id` 是单值外键，一条流水最多一个标签，改成多值要动数据模型 + 导入 + 统计 + 前端，为这点收益不值。
- **兜底桶按"店的形态"分四档，判据机械可判、不用动脑**：便利店（711/lawson/便利蜂）→ 快餐（连锁品牌标准化出品）→ 小吃（非连锁、可外带边走边吃的点心）→ 主食（非连锁、要坐下吃完的碳水正餐，但地域判不出）。**小吃 vs 主食 的界线是"能不能边走边吃"，不是金额**。另有食堂、轻食两个兜底标签维持原样。
- **新增「便利店」标签**：264 条均价 9.3 元的便利店记录混在「快餐」里，把这个标签的均价从 30 拉到 35.9。拆开后快餐 690 条 / 均价 30，便利店 265 条 / 均价 9、最高 36——两个标签各自的统计才有意义。
- **「烧烤」移出便餐小食，与火锅并组为「火锅烧烤」**：烧烤是**做法**不是菜系（东北/新疆/北京都有，判不出归属），性质与火锅完全一致——都是"独立吃法的正餐"。组名只影响走势页左栏归拢展示、不参与任何统计口径（见 daily-expense spec 3.2），所以移组是零风险的纯展示改动。
- **菜系标签的缺口按「相邻区域就近合并」补，不新建单省标签**——这不是新定的规矩，是从库里已有的判例读出来的：`桂老二螺蛳粉`/`桂林小馆` 早就在云贵菜、`大瓦罐`（南昌瓦罐汤）早就在湘鄂菜、`秦晋面庄` 早就在西北菜。现有的「云贵 / 湘鄂 / 京鲁」本身就是这么合并出来的。据此：桂→云贵、赣→湘鄂、晋→西北、津→京鲁。
- **能判地域、但无对应菜系标签且本身是早点形态的，留兜底**：河南 9 条（胡辣汤 8 条均价 25-60 元、洛阳牛肉汤 1 条）没有可就近合并的对象——西北已吸收晋、湘鄂已吸收赣，再塞就散了；单建「豫菜」只为 9 条又不划算。同理沙县（闽）1 条。这是对上面那条判据的**明确例外**，不是遗漏。
- **判不准就不改，宁可兜底桶大一点**：给真实餐厅编一个菜系，比把它留在兜底更糟——错的数据会一直错下去，兜底至少是诚实的"未知"。本轮 309 家商家里约 40 家（`钰花溪` 27 条、`面爱面` 18 条、`金谷园` 14 条、`朵宜` 10 条、`食之六七` 7 条…）判不出，全部留在兜底等后续人工认领。空备注记录（食堂 142 条、快餐 231 条）永久留兜底。

**本轮结果**：中餐菜系 1246 → 1389 条，兜底桶 1839 → 1676 条；主食 108 → 58 条（218 元的饺子馆已清出），快餐 1077 → 690 条、均价回到 30。改库前已备份 `instance/travel.db`。

## 2026-07-29 · D33：中餐菜系体检——取消「海鲜」、闽并入潮汕、纠正一处 2.7 万元的错分

**背景**：D32 定完判据后回头查 1389 条中餐菜系，发现错分不只是零星几条，有两处是结构性的。

**决策**：

- **`新荣记` 从粤菜改判苏浙菜**。新荣记是台州菜起家，9 条 + `新荣记 宝格丽` 1 条合计约 2.7 万元，**占改前粤菜总额 73013 的 37%**——一家店的错分就能让整个菜系的统计失真三分之一。改后粤菜 47202、苏浙菜 73847，两个标签的账面互换了位置。**教训：高客单价的店错分一条，抵得上低价店错分几十条；体检要按金额而不是按条数排查。**
- **取消「海鲜」标签**。它是**食材不是菜系**，跟另外 12 个标签不在一个维度上，混在中餐菜系组里本身就是分类轴打架。15 条拆完各归各位：`渝是乎`/`酸菜鱼`/`烤鱼`→川菜、`灶台鱼`→东北菜、`旺顺阁`（鱼头泡饼）/`北塘老民乐`（天津）→京鲁菜。
  - **`京深`（378 元）单拎出来归「食品」**：它记在「商场」分类下，是去京深海鲜市场买食材回家做，压根不是一顿饭。三餐分类里「买食材回家做」总共只有 `海鲜市场` 2 条，不成规模，**不值得为它新建「在家吃」这类标签**，随其余残条并入京鲁菜（5 条/1413 元，对京鲁 235 条/7.7 万的均价影响是 331→327，可忽略）。
- **闽菜并入潮汕菜，标签改名「闽潮汕菜」**，沿用 D32 的「相邻区域就近合并」逻辑。闽的量（`莆田` 10 条、`闽南小海鲜`、`泉州牛肉馆`、`武夷山农家菜`、`阿咚哥姜母鸭`、`沙县` 共约 16 条/3500 元）比徽菜(4)、台湾菜(13)、素菜(14) 都大，不该继续散落在潮汕菜和兜底里。
  - **名字里不带「粤」**：粤菜是独立标签，叫「闽粤潮汕」会和它语义打架。潮汕自成体系、与粤菜并列这个说法站得住，闽挂靠过来即可。
- **顺带清掉「潮汕菜」积压的约 20 条非潮汕记录**——这个标签此前已经变成「东南沿海海鲜菜」的杂烩：温州/台州系（`台州小海鲜`/`虹桥海鲜面`/`陈记虾爆鳝面馆` 等 6 家）→苏浙菜，**顺德/客家系（`顺德干蒸`/`寻味港顺德小馆`/`东江人家`）→粤菜——顺德是粤菜核心产区，却被塞在潮汕下**，胶东系（`胶东小馆`/`威海小渔村`）→京鲁菜。
- **京鲁菜此前在充当「在北京吃了顿饭」的默认兜底**，119 家里混着 `五棵松吃饭`/`君澜晚餐`/`育新餐饮`/`粥`/`饺子馆`/`汤菜馆` 这类**完全没有菜系信息**的记录。处理口径：**金额大的留京鲁**（`君澜晚餐` 1371、`瑞阁晚餐` 248 等，本来就是正经饭局），**小的按 D32 的兜底四档分**（`饺子馆`/`汤菜馆`→主食、`粥`→小吃、`隐北晚餐`→快餐）。
- **内蒙记在西北菜下，维持不动**（`格日勒阿妈`/`九十九顶`/`魁圣阁内蒙稍麦`/`苏布哈` 约 10 条）。内蒙严格说不属西北，但符合就近合并逻辑，改了反而多一个只有 10 条的标签。
- **`鼎泰丰`（10 条/4220 元）留在苏浙菜**。品牌是台湾的、做的却是小笼包这类江南点心，两说；**按「吃的是什么菜」而不是「品牌注册在哪」判**，所以归苏浙。台湾菜因此维持 13 条 / 0.4%。
- **`九华山`（708 元）留在京鲁菜**：可能是北京店名而非安徽九华山，判不准就不动——同 D32。
- **「徽菜」保留独立，不并入苏浙**。它只有 4 条 / 727 元 / 0.2%，是最小的菜系标签，曾考虑并入苏浙菜改叫「苏浙皖菜」（皖南与浙西苏南接壤，符合既有的相邻合并逻辑）。**否掉的理由：徽菜是八大菜系之一，重油重色重火功的路子与苏浙的清淡偏甜差别不小，合并等于承认不关心这个区分**；标签小不等于该消灭，占比低只说明吃得少。`徽湘雅苑` 从湘鄂菜划归徽菜，改后 5 条 / 916 元。
- **`安徽早餐`（9 元，小吃）维持在兜底不动**：能判地域但本身是早点形态，与河南胡辣汤同一口径（见上）。

**发现但未处理**：
- **同店异写 72 组**，会把商家排行拆散。其中 9 组是**跨标签**的（同一家店被打了不同菜系，必有一错），如 `成涛`(京鲁)/`成涛私房菜`(东北)、`春饼`(东北)/`姥姥家春饼店`(京鲁)、`陈记虾爆鳝`(苏浙)/`陈记虾爆鳝面馆`(潮汕)——这几组已随本轮一并纠正。剩下的是同标签内长短写法（`肥福`/`肥福排挡`/`肥福大排档`、`荣膳荷` 三种写法、`翘脚`/`跷脚`、`发生`/`發生`、`淮扬府`/`淮阳府`）。**不能机器批量合并**：`大董`/`小大董` 是两条品牌线，`荣记`/`新荣记`/`福荣记` 是三家不同的店，`羊蝎子`/`旧县羊蝎子` 是泛称与具体店——必须人过一遍。
- **`西贝充值` 2 条 / 1500 元记在午餐·晚餐分类下，维持不动**。一度以为储值卡充值不是一顿饭、属于虚增餐费，**判断错了**：充值之后在西贝的历次消费并没有单独记账，这 1500 元就是那些饭的唯一凭据，记在餐费里才是对的，删了反而丢数据。**预付卡类记录要先问「后续消费有没有另外记」，再决定它算不算一顿饭**。

**本轮结果**：中餐菜系 13 个标签 → 12 个（取消海鲜）；1389 → 1383 条。改库前已备份。

## 2026-07-29 · D34：菜系判据细化——首要看口味，不看品牌注册地

**背景**：D32 定了「能判菜系归菜系」，但没说清「凭什么判」。执行中发现自己在两家店上是**按品牌出身判的**：`喜家德` 归东北菜的理由是"大连起家的东北品牌"，`袁记云饺` 归粤菜的理由是"广州品牌"——两条理由说的都是公司在哪注册，不是盘子里什么味。

**决策**：

- **菜系判据首要看口味——盘子里是什么味，就归什么菜系，不看品牌注册地或创始地**。
  - 据此**移出菜系**：`喜家德`(8 条)、`袁记云饺`(6 条) → 兜底「主食」。两家都是标准化连锁水饺，馅料做法都没有地域特征；东北菜是铁锅炖/锅包肉/杀猪菜那一路，跟虾仁水饺不是一回事。
  - 反向的一批**正好该留在菜系**，因为它们是"品牌在外地、口味是本地"：`西少爷`（北京品牌做肉夹馍→西北）、`没名生煎`（北京品牌做上海生煎→苏浙）、`太二`（广州品牌做酸菜鱼→川菜）、`霸蛮`（北京品牌做湖南米粉→湘鄂）、`云海肴`（北京品牌做云南菜→云贵）。**这条原则的价值就在这里：它同时管住了两个方向的误判，而不只是把连锁都踢出去。**
  - `鼎泰丰` 留苏浙菜（见 D33）正是这条原则的先例：品牌是台湾的、做的是江南点心，按口味归苏浙。
- **错判集中在低客单价的连锁，高价店几乎没有**。查下来的规律：贵餐厅通常确实做自己招牌的地方菜，口味特征明显；而便宜连锁的产品往往已标准化、没有地域口味，这时人就容易退回去抓"品牌出身"这个看似客观的锚。**以后体检优先扫菜系标签里均价 80 元以下的条目。**
- **对「饺子/面/粉」这类本身无地域归属的品类，判据下沉到「这一家的做法或馅料有没有地域特征」**：`老边饺子` 的煸馅蒸饺是沈阳做法 → 留东北菜；`船歌水饺` 的鲅鱼馅是胶东海鲜特色 → 留京鲁菜；`喜家德`/`袁记云饺` 标准化无特征 → 移出。**同一个品类里，有的归菜系有的归兜底，这不是双标，是因为判据判的是这一家而不是这个品类。**
- **`桃园眷村` 移出台湾菜 → 小吃**。它是上海创立的品牌，打"台湾眷村"概念卖豆浆油条烧饼饭团——**品牌出身和口味两头都不指向台湾**。台湾菜因此从 13 条降到 6 条。
- **`沙县` 移出闽潮汕菜 → 小吃**：沙县小吃早已标准化成全国通用快餐，名号指向福建但口味不再是。**`新兴园` 保留东北菜**：沈阳老字号、非标准化连锁，做法上留着东北特征。
- **泛称记录一律回兜底**：`酒店`（79 元，挂在东北菜下）这类连吃了什么都看不出来的，没有任何口味信息可判。

**本轮结果**：东北菜 45→36 条、粤菜 134→128 条、台湾菜 13→6 条，兜底「主食」58→75 条。

## 2026-07-29 · D35：台湾菜并入闽潮汕，改名「闽台潮汕菜」——小不是合并的理由，杂才是

**背景**：D34 把 `桃园眷村` 移出后，台湾菜只剩 6 条 / 973 元、实际 3 家店（`度小月`+`杜小月` 是同一家的两种写法、`老张牛肉面`、`台北香囤`），体量与刚刚决定保留的徽菜（5 条 / 916 元）几乎一样。

**决策**：

- **台湾菜并入闽潮汕菜，标签改名「闽台潮汕菜」**。依据不是地理凑合，是**口味同源**：潮汕、闽南、台湾三地同属闽南语文化圈（潮汕话与闽南话同系，台湾话本身就是闽南话），饮食共享海鲜、粥、卤味、沙茶、蚵仔这套底子；台菜的主干——担仔面、卤肉饭、蚵仔煎、麻油鸡——就是漳泉移民带过去的闽南菜。合并后 117 条 / 32931 元。
- **代价写明**：台菜有两层跟闽菜无关的东西——1949 年后的**眷村外省菜**（`老张牛肉面` 就是典型，台湾牛肉面是四川老兵在眷村发明的川味红烧）和日据时期的**日式影响**（关东煮、生鱼片那一路）。合并后这两层再也看不出来了。接受这个代价，是因为现有 6 条里主体是担仔面（4 条），恰恰是最该并入闽的那部分。
- **为什么徽菜保留、台湾菜合并——差别不在体量，在纯度**：徽菜 5 条全都是徽菜，小而**纯**；台湾菜 6 条内部分属闽南底子与眷村外省菜两个来源，小而**杂**。**一个标签该不该留，看的是它内部是否同质、能不能一句话说清它代表什么口味，而不是它占多少百分比。**占比低只说明吃得少（见 D33 徽菜条），内部不同质才是标签失效的信号。

**遗留**：`度小月`(3 条) / `杜小月`(1 条) 是同一家的两种写法（`杜` 疑为笔误），属 D33 记的「同店异写 72 组」，待人工确认后合并。

**本轮结果**：中餐菜系 12 个标签 → 11 个。

## 2026-07-29 · D36：裸 SQL 改标签会悄悄废掉指纹——批量整理后必须重刷

**踩坑**：D32–D35 这一轮标签整理全是直接对 `instance/travel.db` 跑 UPDATE 改 `tag_id`，改完备注笔误又跑了一轮 UPDATE 改 `note`。事后才想起 **`tag_name` 和 `note` 都在自然键里**（`natural_key()`，见 D31），也就是说每改一条的标签或备注，它的指纹就对不上了。跑 `refresh_all_fingerprints()` 一刷，**774 条指纹需要更新**——这些记录当时已经处于"再导同一份账单会被当成新记录重复插入"的状态。

**决策**：

- **凡是绕过应用层直接改库的批量整理（改标签、改备注、合并商家写法），收尾必须跑一次 `refresh_all_fingerprints()` 并 commit**。应用层的四个写入入口（新增/编辑/批量改/删除）都自带指纹维护，裸 SQL 没有——这是走捷径的代价，不是 bug。
- **刷完必须再刷一次做幂等自检，第二次的 changed 必须是 0**。D31 已经把重刷设计成幂等的，第二次非 0 就说明有东西不对，别当没看见。
- **改备注比改标签更危险**：改标签只影响自然键里的一个字段，而备注往往是批量归一（把 5 种写法并成 1 种），一次动的记录多、跨的年份广，失效面更大。

**教训**：走裸 SQL 是对的选择（这类整理用应用层 UI 一条条点不现实），但**得知道自己绕过了什么**。这个库里绕过的是指纹维护；下次动别的表之前，先问一句"应用层在这个写入路径上还额外做了什么"。
