# 人生足迹总览 · 年度报告 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增人生足迹总览页 `/insights/` 与年度报告 `/insights/<year>`，共享一套聚合层；旅程列表页的跨旅程看板整体搬到总览页。

**Architecture:** 新增 `services/lifetime.py` 作为唯一聚合层（口径只定义一次），复用现有 `stats.trips_overview` / `stats.to_cny` / `distance.trip_distance_km`，不重写金额与里程逻辑。新增 `blueprints/insights.py` 承载两条只读路由。年度报告的地图第三次复用 `story-map.js` 的 `window.STORY_MAP.render()`。

**Tech Stack:** Python 3 + Flask + SQLAlchemy + Jinja2 + Chart.js 4（CDN，见 base.html）+ d3/topojson（vendor 离线）+ pytest。

设计见 [docs/specs/2026-07-17-insights-design.md](../specs/2026-07-17-insights-design.md)。

## Global Constraints

- **不动数据模型**：无新表、无字段、无迁移。两条路由纯只读。
- **金额一律 `Decimal`**，换算走 `stats.to_cny`（`人民币 = 外币 ÷ 汇率`，两位四舍五入）。禁止在本轮新写换算逻辑。
- **里程走 `distance.trip_distance_km`**（Leg 端点大圆距离累加，缺坐标的段跳过）。禁止新写距离累加。
- **年份归属**：整趟按 `trip.start_date.year`，不按天拆分。
- **在途天数**：`(end_date - start_date).days + 1` 求和，重叠日期去重。
- **新解锁国家**：该国家首次到访旅程的 `start_date` 年份。
- **国家计数**：`City.country` 字段值原样计数，同 `trips_overview` 既有口径。
- **`HOME_CITY = "北京"`**：`most_visited_city` 的排除项、`farthest_city` 的距离基准。与 `trips_overview` 里 top_cities 排除北京的既有口径一致。
- **内联 `<script>` 里的用户自由文本**（旅程标题、城市名）必须走 `_safe_json` 转义，同 `trips.py:424`。
- **TDD**：每个任务先写失败测试，跑到红，再实现，跑到绿，再提交。
- 测试跑法：`pytest tests/test_lifetime.py -v`（项目根目录，已激活 `.venv`）。

---

### Task 1: 聚合层骨架 — 累计数字与在途天数

**Files:**
- Create: `app/services/lifetime.py`
- Test: `tests/test_lifetime.py`

**Interfaces:**
- Consumes: `app.services.stats.trips_overview(trips)` → dict（含 `grand_total` / `trip_count` / `city_count` / `country_count` / `countries` / `global_by_category` / `most_expensive` / `cheapest` / `top_cities` / `trips`）；`app.services.distance.trip_distance_km(trip)` → int
- Produces:
  - `lifetime_stats(trips) -> dict`——本任务先产出 `trips_overview` 的全部键 + `total_distance_km: int` + `days_on_road: int`
  - `_days_on_road(trips) -> int`（模块内私有，Task 3 复用）
  - `_sorted(trips) -> list`（模块内私有，按 `(start_date, id)` 升序，Task 2/3/4 复用）

- [ ] **Step 1: 写失败测试**

创建 `tests/test_lifetime.py`：

```python
import datetime as dt
from decimal import Decimal

from app.models.city import City
from app.models.day import Day, Entry
from app.models.trip import Trip, TripCurrency, Leg
from app.services import lifetime


def make_city(session, name, country="中国", lat=None, lng=None):
    c = City(name=name, country=country, latitude=lat, longitude=lng)
    session.add(c)
    session.flush()
    return c


def make_trip(session, title, start, end, legs=(), days=(), currencies=()):
    """legs: [(from_city, to_city)]；days: [(date, city, [(类别, 标题, 金额, 币种)])]"""
    t = Trip(title=title, start_date=start, end_date=end)
    t.currencies = [TripCurrency(currency_code=code, rate=Decimal(rate))
                    for code, rate in currencies]
    t.legs = [Leg(seq=i, from_city=f, to_city=to, transport_mode="飞机")
              for i, (f, to) in enumerate(legs)]
    day_objs = []
    for date, city, entries in days:
        d = Day(date=date, city=city)
        d.entries = [Entry(category=cat, title=title_, amount=Decimal(amt),
                           currency_code=cur)
                     for cat, title_, amt, cur in entries]
        day_objs.append(d)
    t.days = day_objs
    session.add(t)
    session.commit()
    return t


def test_lifetime_stats_totals(session):
    bj = make_city(session, "北京", lat=39.90, lng=116.41)
    okinawa = make_city(session, "冲绳", country="日本", lat=26.21, lng=127.68)
    t = make_trip(
        session, "冲绳", dt.date(2026, 1, 1), dt.date(2026, 1, 3),
        legs=[(bj, okinawa)],
        days=[(dt.date(2026, 1, 1), okinawa, [("吃饭", "拉面", "2000", "JPY")])],
        currencies=[("JPY", "20")],
    )
    s = lifetime.lifetime_stats([t])
    # 透传 trips_overview
    assert s["trip_count"] == 1
    assert s["grand_total"] == Decimal("100.00")   # 2000 JPY / 20
    assert s["country_count"] == 2                 # 中国 + 日本
    # 新增：里程与在途天数
    assert s["total_distance_km"] > 0
    assert s["days_on_road"] == 3                  # 1/1 ~ 1/3 含首尾


def test_days_on_road_dedupes_overlap(session):
    c = make_city(session, "上海")
    t1 = make_trip(session, "a", dt.date(2026, 3, 1), dt.date(2026, 3, 3))
    t2 = make_trip(session, "b", dt.date(2026, 3, 3), dt.date(2026, 3, 4))
    # 3/1,3/2,3/3 + 3/3,3/4 → 3/3 只算一次 → 4 天
    assert lifetime.lifetime_stats([t1, t2])["days_on_road"] == 4


def test_lifetime_stats_empty(session):
    s = lifetime.lifetime_stats([])
    assert s["trip_count"] == 0
    assert s["days_on_road"] == 0
    assert s["total_distance_km"] == 0
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest tests/test_lifetime.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'app.services.lifetime'`

- [ ] **Step 3: 实现**

创建 `app/services/lifetime.py`：

```python
"""人生足迹总览与年度报告的聚合层。

两个页面（/insights/ 与 /insights/<year>）共用这一层，口径只在这里定义一次：

- 年份归属：整趟按 start_date 的年份，不按天拆分。库里仅 1 趟跨年旅程
  （201612 - 台北，2016-12-29 → 2017-01-02），按天拆会让同一趟同时出现在
  两年的报告里，叙事别扭且旅程数与总览页对不上，为 2 天不值得。见 DECISIONS D21。
- 在途天数：(end_date - start_date + 1) 求和，重叠日期去重（当前数据无重叠，防御性）。
- 新解锁国家：该国家首次到访旅程的 start_date 年份。
- 国家计数：City.country 字段值原样计数，同 stats.trips_overview 既有口径。

金额换算复用 services.stats，里程复用 services.distance，均不重写。
设计见 docs/specs/2026-07-17-insights-design.md。
"""
import datetime as dt

from app.services.distance import trip_distance_km
from app.services.stats import trips_overview

# 住处：most_visited_city 的排除项、farthest_city 的距离基准。
# 与 stats.trips_overview 里 top_cities 排除北京的口径一致。
HOME_CITY = "北京"
CHINA = "中国"


def _sorted(trips):
    """按开始日期升序，同日期按 id 兜底（同 blueprints/trips.py 的相邻旅程口径）。"""
    return sorted(trips, key=lambda t: (t.start_date, t.id))


def _trip_dates(trip):
    """一趟旅程覆盖的日期集合，含首尾。"""
    n = (trip.end_date - trip.start_date).days
    return {trip.start_date + dt.timedelta(days=i) for i in range(n + 1)}


def _days_on_road(trips):
    """在途天数：各旅程日期跨度并集的大小（重叠日期只算一天）。"""
    days = set()
    for t in trips:
        days |= _trip_dates(t)
    return len(days)


def lifetime_stats(trips):
    """人生足迹总览页数据：trips_overview 的全部键 + 里程/在途天数/里程碑/逐年。"""
    overview = trips_overview(trips)
    return {
        **overview,
        "total_distance_km": sum(trip_distance_km(t) for t in trips),
        "days_on_road": _days_on_road(trips),
    }
```

- [ ] **Step 4: 跑测试确认通过**

Run: `pytest tests/test_lifetime.py -v`
Expected: PASS（3 passed）

- [ ] **Step 5: 提交**

```bash
git add app/services/lifetime.py tests/test_lifetime.py
git commit -m "feat: 足迹聚合层骨架——累计里程与在途天数"
```

---

### Task 2: 里程碑

**Files:**
- Modify: `app/services/lifetime.py`
- Test: `tests/test_lifetime.py`

**Interfaces:**
- Consumes: Task 1 的 `_sorted(trips)`、`HOME_CITY`、`CHINA`；`distance.haversine_km(lat1, lng1, lat2, lng2) -> float`
- Produces: `lifetime_stats(trips)["milestones"]` → dict，五个键，各自数据不足时为 `None`：
  - `first_abroad`: `{trip_id, title, date, countries: [str]}`
  - `farthest_trip`: `{trip_id, title, date, distance_km: int}`
  - `longest_trip`: `{trip_id, title, date, days: int}`
  - `most_visited_city`: `{city, count, first_date, last_date}`
  - `farthest_city`: `{city, country, distance_km: int}`

- [ ] **Step 1: 写失败测试**

追加到 `tests/test_lifetime.py`：

```python
def test_milestones(session):
    bj = make_city(session, "北京", lat=39.90, lng=116.41)
    tokyo = make_city(session, "东京", country="日本", lat=35.68, lng=139.69)
    nyc = make_city(session, "纽约", country="美国", lat=40.71, lng=-74.01)
    # 2024 国内游（3 天）；2025 东京（2 天）；2026 纽约（5 天，最远最长）
    t1 = make_trip(session, "国内", dt.date(2024, 5, 1), dt.date(2024, 5, 3),
                   legs=[(bj, bj)])
    t2 = make_trip(session, "东京", dt.date(2025, 5, 1), dt.date(2025, 5, 2),
                   legs=[(bj, tokyo)])
    t3 = make_trip(session, "纽约", dt.date(2026, 5, 1), dt.date(2026, 5, 5),
                   legs=[(bj, nyc)])
    m = lifetime.lifetime_stats([t1, t2, t3])["milestones"]

    assert m["first_abroad"]["trip_id"] == t2.id      # 东京是最早的出国
    assert m["first_abroad"]["countries"] == ["日本"]
    assert m["farthest_trip"]["trip_id"] == t3.id     # 北京→纽约最远
    assert m["longest_trip"]["trip_id"] == t3.id      # 5 天
    assert m["longest_trip"]["days"] == 5
    assert m["farthest_city"]["city"] == "纽约"
    assert m["farthest_city"]["distance_km"] > 10000


def test_most_visited_city_excludes_home(session):
    bj = make_city(session, "北京", lat=39.90, lng=116.41)
    sh = make_city(session, "上海", lat=31.23, lng=121.47)
    # 北京出现 2 次、上海 1 次，但北京是住处要排除 → 上海胜出
    t1 = make_trip(session, "a", dt.date(2025, 1, 1), dt.date(2025, 1, 2),
                   legs=[(bj, sh)])
    t2 = make_trip(session, "b", dt.date(2026, 1, 1), dt.date(2026, 1, 2),
                   legs=[(bj, bj)])
    m = lifetime.lifetime_stats([t1, t2])["milestones"]
    assert m["most_visited_city"]["city"] == "上海"
    assert m["most_visited_city"]["count"] == 1
    assert m["most_visited_city"]["first_date"] == dt.date(2025, 1, 1)
    assert m["most_visited_city"]["last_date"] == dt.date(2025, 1, 1)


def test_milestones_none_when_data_missing(session):
    # 只有国内游、且城市无坐标 → 出国/最远旅程/最远城市都无从谈起
    c = make_city(session, "天津")
    t = make_trip(session, "a", dt.date(2026, 1, 1), dt.date(2026, 1, 1),
                  legs=[(c, c)])
    m = lifetime.lifetime_stats([t])["milestones"]
    assert m["first_abroad"] is None
    assert m["farthest_trip"] is None      # 无坐标 → 里程 0 → 不算里程碑
    assert m["farthest_city"] is None      # 库里无北京 → 无基准
    assert m["longest_trip"]["days"] == 1  # 这个总有


def test_milestones_empty(session):
    m = lifetime.lifetime_stats([])["milestones"]
    assert all(v is None for v in m.values())
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest tests/test_lifetime.py -k milestone -v`
Expected: FAIL — `KeyError: 'milestones'`

- [ ] **Step 3: 实现**

在 `app/services/lifetime.py` 的 import 区补两行：

```python
from app.models.city import City
from app.services.distance import haversine_km, trip_distance_km
```

（把原来的 `from app.services.distance import trip_distance_km` 替换成上面这行。）

在 `lifetime_stats` 之前插入：

```python
def _trip_countries(trip):
    """一趟旅程涉及的国家集合（Trip.cities 由 Leg 推导，见 models/trip.py）。"""
    return {c.country for c in trip.cities if c.country}


def _first_abroad(trips):
    """最早一趟含非中国城市的旅程。"""
    for t in _sorted(trips):
        abroad = sorted(c for c in _trip_countries(t) if c != CHINA)
        if abroad:
            return {"trip_id": t.id, "title": t.title,
                    "date": t.start_date, "countries": abroad}
    return None


def _farthest_trip(trips):
    """里程最长的旅程。全程无坐标（里程 0）的旅程不参与。"""
    best = None
    for t in _sorted(trips):
        km = trip_distance_km(t)
        if km <= 0:
            continue
        if best is None or km > best["distance_km"]:
            best = {"trip_id": t.id, "title": t.title,
                    "date": t.start_date, "distance_km": km}
    return best


def _longest_trip(trips):
    """在途天数最多的旅程。"""
    best = None
    for t in _sorted(trips):
        days = (t.end_date - t.start_date).days + 1
        if best is None or days > best["days"]:
            best = {"trip_id": t.id, "title": t.title,
                    "date": t.start_date, "days": days}
    return best


def _most_visited_city(trips):
    """覆盖旅程数最多的城市（排除住处），带首末次到访日期。

    并列时按城市名升序取第一个，与 trips_overview 的 top_cities 排序口径一致。
    """
    visits = {}
    for t in _sorted(trips):
        for name in sorted({c.name for c in t.cities if c.name != HOME_CITY}):
            visits.setdefault(name, []).append(t.start_date)
    if not visits:
        return None
    name = min(visits, key=lambda n: (-len(visits[n]), n))
    dates = visits[name]   # _sorted 保证已按日期升序
    return {"city": name, "count": len(dates),
            "first_date": dates[0], "last_date": dates[-1]}


def _farthest_city(trips):
    """距住处直线距离最大的城市。住处不在库里或无坐标时无基准，返回 None。"""
    home = City.query.filter_by(name=HOME_CITY).first()
    if home is None or home.latitude is None or home.longitude is None:
        return None
    best = None
    seen = set()
    for t in trips:
        for c in t.cities:
            if c.id in seen or c.latitude is None or c.name == HOME_CITY:
                continue
            seen.add(c.id)
            km = round(haversine_km(home.latitude, home.longitude,
                                    c.latitude, c.longitude))
            if best is None or km > best["distance_km"]:
                best = {"city": c.name, "country": c.country, "distance_km": km}
    return best


def _milestones(trips):
    return {
        "first_abroad": _first_abroad(trips),
        "farthest_trip": _farthest_trip(trips),
        "longest_trip": _longest_trip(trips),
        "most_visited_city": _most_visited_city(trips),
        "farthest_city": _farthest_city(trips),
    }
```

在 `lifetime_stats` 的返回 dict 里加一行 `"milestones": _milestones(trips),`：

```python
def lifetime_stats(trips):
    """人生足迹总览页数据：trips_overview 的全部键 + 里程/在途天数/里程碑/逐年。"""
    overview = trips_overview(trips)
    return {
        **overview,
        "total_distance_km": sum(trip_distance_km(t) for t in trips),
        "days_on_road": _days_on_road(trips),
        "milestones": _milestones(trips),
    }
```

- [ ] **Step 4: 跑测试确认通过**

Run: `pytest tests/test_lifetime.py -v`
Expected: PASS（7 passed）

- [ ] **Step 5: 提交**

```bash
git add app/services/lifetime.py tests/test_lifetime.py
git commit -m "feat: 足迹里程碑——首次出国/最远/最长/最常去/离家最远"
```

---

### Task 3: 逐年序列

**Files:**
- Modify: `app/services/lifetime.py`
- Test: `tests/test_lifetime.py`

**Interfaces:**
- Consumes: Task 1 的 `_sorted` / `_days_on_road`；Task 2 的 `_trip_countries`；`stats.trip_stats(trip)["total_cny"]`
- Produces:
  - `lifetime_stats(trips)["by_year"]` → `[{year, trip_count, total_cny: Decimal, distance_km: int, days_on_road: int, new_countries: [str]}]`，年份连续（空档补零），按年升序
  - `lifetime_stats(trips)["busiest_year"]` → int 或 `None`
  - `_year_rows(trips)`、`_first_year_by_country(trips)`（模块内私有，Task 4 复用）

- [ ] **Step 1: 写失败测试**

追加到 `tests/test_lifetime.py`：

```python
def test_by_year_fills_gap_years(session):
    c = make_city(session, "上海")
    t1 = make_trip(session, "a", dt.date(2023, 1, 1), dt.date(2023, 1, 2), legs=[(c, c)])
    t2 = make_trip(session, "b", dt.date(2026, 1, 1), dt.date(2026, 1, 2), legs=[(c, c)])
    rows = lifetime.lifetime_stats([t1, t2])["by_year"]
    # 2024、2025 无旅程也要补零，趋势图才看得出停摆
    assert [r["year"] for r in rows] == [2023, 2024, 2025, 2026]
    assert rows[1]["trip_count"] == 0
    assert rows[1]["total_cny"] == Decimal("0.00")
    assert rows[1]["days_on_road"] == 0
    assert rows[1]["new_countries"] == []


def test_cross_year_trip_belongs_to_start_year(session):
    bj = make_city(session, "北京", lat=39.90, lng=116.41)
    tp = make_city(session, "台北", country="台湾", lat=25.03, lng=121.57)
    # 2016-12-29 → 2017-01-02：整趟归 2016，不按天拆
    t = make_trip(session, "台北", dt.date(2016, 12, 29), dt.date(2017, 1, 2),
                  legs=[(bj, tp)],
                  days=[(dt.date(2017, 1, 1), tp, [("吃饭", "牛肉面", "100", "CNY")])],
                  currencies=[])
    rows = {r["year"]: r for r in lifetime.lifetime_stats([t])["by_year"]}
    assert rows[2016]["trip_count"] == 1
    assert rows[2016]["days_on_road"] == 5          # 12/29~1/2 整段算 2016
    assert rows[2016]["total_cny"] == Decimal("100.00")   # 2017-01-01 的消费也归 2016
    assert rows[2016]["new_countries"] == ["中国", "台湾"]
    assert 2017 not in rows                          # 只有这一趟，年份跨度到 2016 为止


def test_new_countries_by_first_visit_start_year(session):
    bj = make_city(session, "北京", lat=39.90, lng=116.41)
    tokyo = make_city(session, "东京", country="日本", lat=35.68, lng=139.69)
    t1 = make_trip(session, "东京 1", dt.date(2025, 1, 1), dt.date(2025, 1, 2),
                   legs=[(bj, tokyo)])
    t2 = make_trip(session, "东京 2", dt.date(2026, 1, 1), dt.date(2026, 1, 2),
                   legs=[(bj, tokyo)])
    rows = {r["year"]: r for r in lifetime.lifetime_stats([t1, t2])["by_year"]}
    assert rows[2025]["new_countries"] == ["中国", "日本"]
    assert rows[2026]["new_countries"] == []   # 第二次去日本不算新解锁


def test_busiest_year(session):
    c = make_city(session, "上海")
    trips = [
        make_trip(session, "a", dt.date(2025, 1, 1), dt.date(2025, 1, 2), legs=[(c, c)]),
        make_trip(session, "b", dt.date(2026, 1, 1), dt.date(2026, 1, 2), legs=[(c, c)]),
        make_trip(session, "c", dt.date(2026, 3, 1), dt.date(2026, 3, 2), legs=[(c, c)]),
    ]
    assert lifetime.lifetime_stats(trips)["busiest_year"] == 2026


def test_by_year_empty(session):
    s = lifetime.lifetime_stats([])
    assert s["by_year"] == []
    assert s["busiest_year"] is None
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest tests/test_lifetime.py -k year -v`
Expected: FAIL — `KeyError: 'by_year'`

- [ ] **Step 3: 实现**

在 `app/services/lifetime.py` 的 import 区把 stats 那行改成：

```python
from app.services.stats import to_cny, trip_stats, trips_overview
```

（`to_cny` 供 Task 4 用，本任务先一起引入。）并补：

```python
from decimal import Decimal
```

在 `_milestones` 之后插入：

```python
def _first_year_by_country(trips):
    """{国家: 首次到访年份}——按旅程 start_date 的年份归属。"""
    first = {}
    for t in _sorted(trips):
        for country in _trip_countries(t):
            first.setdefault(country, t.start_date.year)
    return first


def _year_rows(trips):
    """逐年聚合，年份连续（空档年补零）。空档年本身是信息（如疫情停摆）。"""
    by_year = {}
    for t in trips:
        by_year.setdefault(t.start_date.year, []).append(t)
    if not by_year:
        return []

    new_by_year = {}
    for country, year in _first_year_by_country(trips).items():
        new_by_year.setdefault(year, []).append(country)

    rows = []
    for year in range(min(by_year), max(by_year) + 1):
        year_trips = by_year.get(year, [])
        total = sum((trip_stats(t)["total_cny"] for t in year_trips), Decimal("0.00"))
        rows.append({
            "year": year,
            "trip_count": len(year_trips),
            "total_cny": total,
            "distance_km": sum(trip_distance_km(t) for t in year_trips),
            "days_on_road": _days_on_road(year_trips),
            "new_countries": sorted(new_by_year.get(year, [])),
        })
    return rows


def _busiest_year(rows):
    """旅程数最多的年份；并列取最早那年。"""
    if not rows:
        return None
    return min(rows, key=lambda r: (-r["trip_count"], r["year"]))["year"]
```

`lifetime_stats` 补两个键：

```python
def lifetime_stats(trips):
    """人生足迹总览页数据：trips_overview 的全部键 + 里程/在途天数/里程碑/逐年。"""
    overview = trips_overview(trips)
    rows = _year_rows(trips)
    return {
        **overview,
        "total_distance_km": sum(trip_distance_km(t) for t in trips),
        "days_on_road": _days_on_road(trips),
        "milestones": _milestones(trips),
        "by_year": rows,
        "busiest_year": _busiest_year(rows),
    }
```

- [ ] **Step 4: 跑测试确认通过**

Run: `pytest tests/test_lifetime.py -v`
Expected: PASS（12 passed）

- [ ] **Step 5: 提交**

```bash
git add app/services/lifetime.py tests/test_lifetime.py
git commit -m "feat: 足迹逐年序列——空档年补零、跨年旅程归开始年"
```

---

### Task 4: 年度报告数据

**Files:**
- Modify: `app/services/lifetime.py`
- Test: `tests/test_lifetime.py`

**Interfaces:**
- Consumes: Task 1 的 `_sorted` / `_days_on_road`；Task 2 的 `_trip_countries` / `_farthest_trip`；Task 3 的 `_first_year_by_country` / `_year_rows`；`stats.to_cny(amount, code, rate_map)`；`story._point(city)` → `{lat, lng, name}` 或 `None`
- Produces: `year_report(trips, year) -> dict | None`（该年无旅程时 `None`），键：
  - `year: int`、`years: [int]`（有旅程的年份升序，供年份切换器）、`prev_year: int|None`、`next_year: int|None`
  - `trip_count` / `country_count` / `total_cny: Decimal` / `days_on_road` / `distance_km`
  - `map: {route, cities}`
  - `new_countries: [str]`
  - `trips: [{id, title, start_date, end_date, cities: [str], total_cny}]`
  - `by_category: {类别: Decimal}`、`prev_total_cny: Decimal|None`
  - `superlatives: {best_meal, best_stay, biggest_day, farthest_trip}`（各自可为 `None`）

- [ ] **Step 1: 写失败测试**

追加到 `tests/test_lifetime.py`：

```python
def test_year_report_sections(session):
    bj = make_city(session, "北京", lat=39.90, lng=116.41)
    tokyo = make_city(session, "东京", country="日本", lat=35.68, lng=139.69)
    prev = make_trip(session, "去年", dt.date(2025, 6, 1), dt.date(2025, 6, 2),
                     legs=[(bj, bj)],
                     days=[(dt.date(2025, 6, 1), bj, [("购物", "书", "50", "CNY")])])
    t = make_trip(
        session, "东京", dt.date(2026, 4, 1), dt.date(2026, 4, 3),
        legs=[(bj, tokyo)],
        days=[
            (dt.date(2026, 4, 1), tokyo, [("吃饭", "寿司", "4000", "JPY"),
                                          ("住宿", "酒店", "6000", "JPY")]),
            (dt.date(2026, 4, 2), tokyo, [("吃饭", "拉面", "2000", "JPY")]),
        ],
        currencies=[("JPY", "20")],
    )
    r = lifetime.year_report([prev, t], 2026)

    # 第 1 节 封面
    assert r["year"] == 2026
    assert r["trip_count"] == 1
    assert r["country_count"] == 2          # 中国 + 日本
    assert r["days_on_road"] == 3
    assert r["total_cny"] == Decimal("600.00")   # (4000+6000+2000)/20
    # 年份切换器
    assert r["years"] == [2025, 2026]
    assert r["prev_year"] == 2025
    assert r["next_year"] is None
    # 第 2 节 地图
    assert {c["name"] for c in r["map"]["cities"]} == {"北京", "东京"}
    assert len(r["map"]["route"]) == 1
    # 第 3 节 新解锁国家
    assert r["new_countries"] == ["日本"]   # 中国在 2025 已解锁
    # 第 4 节 旅程清单
    assert [x["id"] for x in r["trips"]] == [t.id]
    assert r["trips"][0]["total_cny"] == Decimal("600.00")
    # 第 5 节 花费构成 + 同比
    assert r["by_category"]["吃饭"] == Decimal("300.00")
    assert r["by_category"]["住宿"] == Decimal("300.00")
    assert r["prev_total_cny"] == Decimal("50.00")
    # 第 6 节 年度之最
    s = r["superlatives"]
    assert s["best_meal"]["title"] == "寿司"
    assert s["best_meal"]["cny"] == Decimal("200.00")
    assert s["best_stay"]["title"] == "酒店"
    assert s["biggest_day"]["date"] == dt.date(2026, 4, 1)   # 500 > 100
    assert s["biggest_day"]["cny"] == Decimal("500.00")
    assert s["farthest_trip"]["trip_id"] == t.id


def test_year_report_missing_year(session):
    c = make_city(session, "上海")
    t = make_trip(session, "a", dt.date(2026, 1, 1), dt.date(2026, 1, 2), legs=[(c, c)])
    assert lifetime.year_report([t], 2020) is None
    assert lifetime.year_report([], 2026) is None


def test_year_report_prev_total_none_when_no_prev_year(session):
    c = make_city(session, "上海")
    t = make_trip(session, "a", dt.date(2026, 1, 1), dt.date(2026, 1, 2), legs=[(c, c)])
    r = lifetime.year_report([t], 2026)
    assert r["prev_total_cny"] is None
    assert r["prev_year"] is None


def test_year_report_superlatives_none_without_entries(session):
    c = make_city(session, "上海")
    t = make_trip(session, "a", dt.date(2026, 1, 1), dt.date(2026, 1, 2), legs=[(c, c)])
    s = lifetime.year_report([t], 2026)["superlatives"]
    assert s["best_meal"] is None
    assert s["best_stay"] is None
    assert s["biggest_day"] is None
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest tests/test_lifetime.py -k year_report -v`
Expected: FAIL — `AttributeError: module 'app.services.lifetime' has no attribute 'year_report'`

- [ ] **Step 3: 实现**

在 `app/services/lifetime.py` 的 import 区补：

```python
from app.models.day import CATEGORIES
# _point 复用故事页的城市点整形（有经纬度才算有效），避免两处各写一遍。
from app.services.story import _point
```

在文件末尾追加：

```python
def _year_map(year_trips):
    """那年所有旅程的路线叠在一张图上，喂给 story-map.js 的 STORY_MAP.render()。

    不带 day_cities：年度报告没有「按天高亮」，只画全年足迹。
    """
    route, cities, seen = [], [], set()
    for t in year_trips:
        for leg in t.legs:   # Trip.legs 已按 seq 排序
            frm, to = _point(leg.from_city), _point(leg.to_city)
            if frm is None or to is None:
                continue
            route.append({"from": frm, "to": to})
            for p in (frm, to):
                if p["name"] not in seen:
                    seen.add(p["name"])
                    cities.append(dict(p))
    for t in year_trips:
        for d in t.days:
            # 不在任何 Leg 端点上的天也画成点（同 story._map_data 的口径）
            p = _point(d.city)
            if p and p["name"] not in seen:
                seen.add(p["name"])
                cities.append(dict(p))
    return {"route": route, "cities": cities}


def _superlatives(year_trips):
    """年度之最：最贵的一顿饭 / 最贵的一次住宿 / 花得最多的一天 / 走得最远的一趟。"""
    best = {"best_meal": None, "best_stay": None, "biggest_day": None}
    cat_key = {"吃饭": "best_meal", "住宿": "best_stay"}
    for t in year_trips:
        rate_map = {c.currency_code: Decimal(c.rate) for c in t.currencies}
        for d in sorted(t.days, key=lambda x: x.date):
            day_total = Decimal("0.00")
            for e in d.entries:
                cny = to_cny(e.amount, e.currency_code, rate_map)
                day_total += cny
                key = cat_key.get(e.category)
                if key and (best[key] is None or cny > best[key]["cny"]):
                    best[key] = {"title": e.title, "cny": cny, "date": d.date,
                                 "trip_id": t.id, "trip_title": t.title}
            if day_total > 0 and (best["biggest_day"] is None
                                  or day_total > best["biggest_day"]["cny"]):
                best["biggest_day"] = {
                    "cny": day_total, "date": d.date,
                    "city": d.city.name if d.city else None,
                    "trip_id": t.id, "trip_title": t.title,
                }
    best["farthest_trip"] = _farthest_trip(year_trips)
    return best


def year_report(trips, year):
    """某一年的年度报告数据；该年无旅程返回 None（路由据此 404）。"""
    year_trips = _sorted([t for t in trips if t.start_date.year == year])
    if not year_trips:
        return None

    years = sorted({t.start_date.year for t in trips})
    idx = years.index(year)

    total = Decimal("0.00")
    by_category = {cat: Decimal("0.00") for cat in CATEGORIES}
    trip_rows = []
    for t in year_trips:
        s = trip_stats(t)
        total += s["total_cny"]
        for cat in CATEGORIES:
            by_category[cat] += s["by_category"][cat]
        trip_rows.append({
            "id": t.id, "title": t.title,
            "start_date": t.start_date, "end_date": t.end_date,
            "cities": [c.name for c in t.cities],
            "total_cny": s["total_cny"],
        })

    prev_year = years[idx - 1] if idx > 0 else None
    prev_total = None
    if prev_year is not None:
        prev_total = sum((trip_stats(t)["total_cny"] for t in trips
                          if t.start_date.year == prev_year), Decimal("0.00"))

    countries = set()
    for t in year_trips:
        countries |= _trip_countries(t)
    new_countries = sorted(c for c, y in _first_year_by_country(trips).items()
                           if y == year)

    return {
        "year": year,
        "years": years,
        "prev_year": prev_year,
        "next_year": years[idx + 1] if idx < len(years) - 1 else None,
        "trip_count": len(year_trips),
        "country_count": len(countries),
        "total_cny": total,
        "days_on_road": _days_on_road(year_trips),
        "distance_km": sum(trip_distance_km(t) for t in year_trips),
        "map": _year_map(year_trips),
        "new_countries": new_countries,
        "trips": trip_rows,
        "by_category": by_category,
        "prev_total_cny": prev_total,
        "superlatives": _superlatives(year_trips),
    }
```

- [ ] **Step 4: 跑测试确认通过**

Run: `pytest tests/test_lifetime.py -v`
Expected: PASS（16 passed）

- [ ] **Step 5: 提交**

```bash
git add app/services/lifetime.py tests/test_lifetime.py
git commit -m "feat: 年度报告数据聚合"
```

---

### Task 5: insights 蓝图 + 总览页（KPI + 里程碑）

本任务先让 `/insights/` 跑起来（KPI 行 + 里程碑行），逐年图与看板搬迁留到 Task 6。

**Files:**
- Create: `app/blueprints/insights.py`
- Create: `app/templates/insights/overview.html`
- Create: `app/templates/insights/_cards.html`
- Create: `tests/test_insights_routes.py`
- Modify: `app/blueprints/__init__.py`
- Modify: `app/templates/base.html:13-17`
- Modify: `app/static/style.css`（追加到文件末尾）

**Interfaces:**
- Consumes: Task 3 完成后的 `lifetime.lifetime_stats(trips)`
- Produces: 路由 `insights.overview`（`GET /insights/`）；模板宏 `_cards.html` 的 `toplist(card, cat_colors)` 与 `comp(card, categories, cat_colors)`（Task 6 搬看板时用）

- [ ] **Step 1: 写失败测试**

创建 `tests/test_insights_routes.py`：

```python
import datetime as dt
from decimal import Decimal

from app.extensions import db
from app.models.city import City
from app.models.day import Day, Entry
from app.models.trip import Trip, TripCurrency, Leg


def seed(app):
    """一趟 2026 东京 + 一趟 2025 国内，供路由测试用。"""
    with app.app_context():
        bj = City(name="北京", country="中国", latitude=39.90, longitude=116.41)
        tokyo = City(name="东京", country="日本", latitude=35.68, longitude=139.69)
        db.session.add_all([bj, tokyo])
        db.session.flush()
        t0 = Trip(title="国内游", start_date=dt.date(2025, 5, 1),
                  end_date=dt.date(2025, 5, 2))
        t0.legs = [Leg(seq=0, from_city=bj, to_city=bj, transport_mode="高铁")]
        t = Trip(title="东京之旅", start_date=dt.date(2026, 4, 1),
                 end_date=dt.date(2026, 4, 3))
        t.currencies = [TripCurrency(currency_code="JPY", rate=Decimal("20"))]
        t.legs = [Leg(seq=0, from_city=bj, to_city=tokyo, transport_mode="飞机")]
        d = Day(date=dt.date(2026, 4, 1), city=tokyo)
        d.entries = [Entry(category="吃饭", title="寿司", amount=Decimal("4000"),
                           currency_code="JPY")]
        t.days = [d]
        db.session.add_all([t0, t])
        db.session.commit()


def test_overview_renders(client, app):
    seed(app)
    resp = client.get("/insights/")
    body = resp.get_data(as_text=True)
    assert resp.status_code == 200
    assert "在途天数" in body
    assert "东京之旅" in body        # 最贵旅程卡（Task 6 搬来）或里程碑
    assert "第一次出国" in body


def test_overview_empty_db(client, app):
    resp = client.get("/insights/")
    assert resp.status_code == 200
    assert "还没有旅程" in resp.get_data(as_text=True)


def test_nav_has_insights_link(client, app):
    resp = client.get("/trips/")
    assert 'href="/insights/"' in resp.get_data(as_text=True)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest tests/test_insights_routes.py -v`
Expected: FAIL — `/insights/` 返回 404

- [ ] **Step 3: 实现**

创建 `app/blueprints/insights.py`：

```python
"""跨旅程的回顾视图：人生足迹总览 + 年度报告。

只读路由，取数全部走 services.lifetime。设计见
docs/specs/2026-07-17-insights-design.md。
"""
from flask import Blueprint, render_template

from app.models.day import CATEGORIES
from app.models.trip import Trip
from app.services.lifetime import lifetime_stats

bp = Blueprint("insights", __name__, url_prefix="/insights")


@bp.route("/")
def overview():
    trips = Trip.query.order_by(Trip.start_date.desc()).all()
    return render_template("insights/overview.html",
                           s=lifetime_stats(trips), categories=CATEGORIES)
```

在 `app/blueprints/__init__.py` 注册：

```python
def register_blueprints(app):
    from .main import bp as main_bp
    from .trips import bp as trips_bp
    from .settings import bp as settings_bp
    from .insights import bp as insights_bp
    app.register_blueprint(main_bp)
    app.register_blueprint(trips_bp)
    app.register_blueprint(settings_bp)
    app.register_blueprint(insights_bp)
```

创建 `app/templates/insights/_cards.html`——把 `trips/list.html` 里写死的两个宏搬过来，改成显式传参（宏跨模板复用时拿不到调用方的 `{% set %}` 变量）：

```jinja
{% macro toplist(card, cat_colors) %}
{% set top10 = card.top_entries[:10] %}
{% if top10 %}
<span class="super-top">
  <span class="super-top-label">最贵消费 Top {{ top10|length }}</span>
  <ol class="super-top-grid">
    {% for e in top10 %}
    <li class="stt-item">
      <i style="background: {{ cat_colors.get(e.category, '#b0a99f') }}"></i>
      <span class="stt-title">{{ e.title }}</span>
      <span class="stt-amount">￥{{ '{:,.0f}'.format(e.cny) }}</span>
    </li>
    {% endfor %}
  </ol>
</span>
{% endif %}
{% endmacro %}

{% macro comp(card, categories, cat_colors) %}
<span class="super-comp">
  <span class="super-comp-label">消费构成</span>
  {% if card.total_cny > 0 %}
  <span class="super-comp-bar" aria-hidden="true">
    {% for cat in categories %}{% if card.by_category[cat] > 0 %}
    <span style="flex: {{ card.by_category[cat] }}; background: {{ cat_colors.get(cat, '#b0a99f') }}"></span>
    {% endif %}{% endfor %}
  </span>
  <span class="super-comp-legend">
    {% for cat in categories %}{% if card.by_category[cat] > 0 %}
    <span class="scl-item"><i style="background: {{ cat_colors.get(cat, '#b0a99f') }}"></i>{{ cat }} {{ (card.by_category[cat] / card.total_cny * 100) | float | round | int }}%</span>
    {% endif %}{% endfor %}
  </span>
  {% else %}
  <span class="super-comp-empty">暂无消费记录</span>
  {% endif %}
</span>
{% endmacro %}
```

创建 `app/templates/insights/overview.html`：

```jinja
{% extends "base.html" %}
{% block title %}人生足迹{% endblock %}
{% set cat_colors = {
  "吃饭": "#e76f51", "游玩": "#2a9d8f", "购物": "#e9c46a",
  "住宿": "#4a9fc9", "交通": "#9b8cb5", "其他消费": "#b0a99f"} %}
{% block content %}
<div class="list-head">
  <h1>人生足迹</h1>
</div>

{% if not s.trip_count %}
<p class="muted">还没有旅程，<a href="{{ url_for('trips.create') }}">去创建一个 →</a></p>
{% else %}
<div class="kpi-row kpi-row-6">
  <div class="card kpi">
    <span class="kpi-label">历史总花费</span>
    <span class="kpi-value">￥{{ '{:,.2f}'.format(s.grand_total) }}</span>
  </div>
  <div class="card kpi">
    <span class="kpi-label">旅程数</span>
    <span class="kpi-value">{{ s.trip_count }}</span>
  </div>
  <div class="card kpi">
    <span class="kpi-label">在途天数</span>
    <span class="kpi-value">{{ '{:,}'.format(s.days_on_road) }}</span>
  </div>
  <div class="card kpi">
    <span class="kpi-label">总里程</span>
    <span class="kpi-value">{{ '{:,}'.format(s.total_distance_km) }} <small>km</small></span>
  </div>
  <div class="card kpi">
    <span class="kpi-label">到访国家</span>
    <span class="kpi-value">{{ s.country_count }}
      <span class="kpi-flags">{% for f in s.countries | map('country_flag') | unique %}{{ f }}{% endfor %}</span>
    </span>
  </div>
  <div class="card kpi">
    <span class="kpi-label">到访城市</span>
    <span class="kpi-value">{{ s.city_count }}</span>
    {% if s.top_cities %}
    <span class="kpi-sub">{% for tc in s.top_cities %}{{ tc.city }} {{ tc.count }}{% if not loop.last %} · {% endif %}{% endfor %}</span>
    {% endif %}
  </div>
</div>

{% set m = s.milestones %}
<div class="ms-row">
  {% if m.first_abroad %}
  <a class="card ms-card" href="{{ url_for('trips.detail', trip_id=m.first_abroad.trip_id) }}">
    <span class="ms-emoji">🛂</span>
    <span class="ms-label">第一次出国</span>
    <span class="ms-value">{{ m.first_abroad.countries | map('country_flag') | join('') }}
      {{ m.first_abroad.countries | join('、') }}</span>
    <span class="ms-sub">{{ m.first_abroad.date }} · {{ m.first_abroad.title }}</span>
  </a>
  {% endif %}
  {% if m.farthest_trip %}
  <a class="card ms-card" href="{{ url_for('trips.detail', trip_id=m.farthest_trip.trip_id) }}">
    <span class="ms-emoji">🛫</span>
    <span class="ms-label">走得最远的一次</span>
    <span class="ms-value">{{ '{:,}'.format(m.farthest_trip.distance_km) }} km</span>
    <span class="ms-sub">{{ m.farthest_trip.date }} · {{ m.farthest_trip.title }}</span>
  </a>
  {% endif %}
  {% if m.longest_trip %}
  <a class="card ms-card" href="{{ url_for('trips.detail', trip_id=m.longest_trip.trip_id) }}">
    <span class="ms-emoji">📅</span>
    <span class="ms-label">最长的一趟</span>
    <span class="ms-value">{{ m.longest_trip.days }} 天</span>
    <span class="ms-sub">{{ m.longest_trip.date }} · {{ m.longest_trip.title }}</span>
  </a>
  {% endif %}
  {% if m.most_visited_city %}
  <div class="card ms-card">
    <span class="ms-emoji">📍</span>
    <span class="ms-label">去得最多的城市</span>
    <span class="ms-value">{{ m.most_visited_city.city }}</span>
    <span class="ms-sub">{{ m.most_visited_city.count }} 次 ·
      {{ m.most_visited_city.first_date }} ~ {{ m.most_visited_city.last_date }}</span>
  </div>
  {% endif %}
  {% if m.farthest_city %}
  <div class="card ms-card">
    <span class="ms-emoji">🌏</span>
    <span class="ms-label">离家最远的城市</span>
    <span class="ms-value">{{ m.farthest_city.country | country_flag }} {{ m.farthest_city.city }}</span>
    <span class="ms-sub">距北京 {{ '{:,}'.format(m.farthest_city.distance_km) }} km</span>
  </div>
  {% endif %}
</div>
{% endif %}
{% endblock %}
```

`app/templates/base.html` 导航栏加「足迹」（放在「旅程」之后）：

```jinja
    <div class="navlinks">
      <a href="{{ url_for('trips.list') }}">旅程</a>
      <a href="{{ url_for('insights.overview') }}">足迹</a>
      <a href="{{ url_for('trips.create') }}">创建</a>
      <a href="{{ url_for('settings.cities') }}">设置</a>
    </div>
```

样式追加到 `app/static/style.css` 末尾：

```css
/* ---- 人生足迹总览：KPI 六列 + 里程碑卡 ---- */
.kpi-row-6 { grid-template-columns: repeat(auto-fit, minmax(150px, 1fr)); }

.ms-row {
  display: grid; grid-template-columns: repeat(auto-fit, minmax(190px, 1fr));
  gap: 12px; margin-bottom: 1.2rem;
}
.ms-card { display: flex; flex-direction: column; gap: 2px; text-decoration: none; color: inherit; }
a.ms-card:hover { transform: translateY(-2px); transition: transform .15s; }
.ms-emoji { font-size: 1.5rem; }
.ms-label { font-size: .8rem; color: #888; }
.ms-value { font-size: 1.15rem; font-weight: 600; }
.ms-sub { font-size: .78rem; color: #999; }
```

- [ ] **Step 4: 跑测试确认通过**

Run: `pytest tests/test_insights_routes.py -v`
Expected: PASS（3 passed）

注：`test_overview_renders` 里 `"东京之旅" in body` 此时靠里程碑卡的 `.ms-sub` 满足（最远/最长旅程都是它）；Task 6 搬来最贵旅程卡后依然满足。

- [ ] **Step 5: 提交**

```bash
git add app/blueprints/insights.py app/blueprints/__init__.py \
        app/templates/insights/ app/templates/base.html \
        app/static/style.css tests/test_insights_routes.py
git commit -m "feat: 人生足迹总览页——KPI 与里程碑"
```

---

### Task 6: 逐年对比图 + 看板搬迁 + 列表页瘦身

**Files:**
- Modify: `app/templates/insights/overview.html`
- Modify: `app/blueprints/insights.py`
- Modify: `app/blueprints/trips.py:70-93`（`list()`）
- Modify: `app/templates/trips/list.html`（整体重写，只留列表）
- Test: `tests/test_insights_routes.py`

**Interfaces:**
- Consumes: Task 5 的 `insights.overview` 路由与 `_cards.html` 宏；Task 3 的 `by_year` / `busiest_year`
- Produces: 总览页含 `#yearChart` / `#compareChart` / `#globalCat` 三张图；`trips.list()` 只向模板传 `trips`

- [ ] **Step 1: 写失败测试**

追加到 `tests/test_insights_routes.py`：

```python
def test_overview_has_year_chart_and_board(client, app):
    seed(app)
    body = client.get("/insights/").get_data(as_text=True)
    assert "逐年对比" in body
    assert 'id="yearChart"' in body
    assert 'id="compareChart"' in body     # 各旅程花费对比（从列表页搬来）
    assert 'id="globalCat"' in body        # 整体消费构成环图
    assert "最贵旅程" in body


def test_list_page_is_list_only(client, app):
    seed(app)
    body = client.get("/trips/").get_data(as_text=True)
    assert "东京之旅" in body            # 列表本体还在
    assert "历史总花费" not in body      # 看板已搬走
    assert 'id="compareChart"' not in body
    assert "最贵旅程" not in body
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest tests/test_insights_routes.py -k "year_chart or list_page" -v`
Expected: FAIL — `assert 'id="yearChart"' in body` 失败；`"历史总花费" not in body` 失败

- [ ] **Step 3: 实现**

`app/blueprints/insights.py` 补内联图表数据（用户自由文本必须转义，见 Global Constraints）：

```python
"""跨旅程的回顾视图：人生足迹总览 + 年度报告。

只读路由，取数全部走 services.lifetime。设计见
docs/specs/2026-07-17-insights-design.md。
"""
import json

from flask import Blueprint, render_template

from app.models.day import CATEGORIES
from app.models.trip import Trip
from app.services.lifetime import lifetime_stats

bp = Blueprint("insights", __name__, url_prefix="/insights")


def _safe_json(obj):
    # 旅程标题/城市名是用户自由文本，要进内联 <script>。转义 < > & 防止 </script>
    # 截断/注入，同时保留 ensure_ascii=False 让中文可读。同 blueprints/trips.py。
    return (json.dumps(obj, ensure_ascii=False)
            .replace("<", "\\u003c").replace(">", "\\u003e").replace("&", "\\u0026"))


@bp.route("/")
def overview():
    trips = Trip.query.order_by(Trip.start_date.desc()).all()
    s = lifetime_stats(trips)
    chart_trips = [{
        "id": r["id"],
        "title": r["title"],
        "total": float(r["total_cny"]),
        "start": r["start_date"].isoformat(),
        "end": r["end_date"].isoformat(),
        "days": (r["end_date"] - r["start_date"]).days + 1,
        "by_category": {cat: float(v) for cat, v in r["by_category"].items()},
    } for r in s["trips"]]
    years = [{
        "year": r["year"],
        "total": float(r["total_cny"]),
        "trip_count": r["trip_count"],
        "distance_km": r["distance_km"],
        "days_on_road": r["days_on_road"],
    } for r in s["by_year"]]
    return render_template(
        "insights/overview.html",
        s=s,
        categories=CATEGORIES,
        chart_trips_json=_safe_json(chart_trips),
        global_cat_json=_safe_json({cat: float(v) for cat, v
                                    in s["global_by_category"].items()}),
        years_json=_safe_json(years),
        busiest_year=s["busiest_year"],
    )
```

`app/templates/insights/overview.html`——在文件顶部 `{% extends %}` 之后加导入，并在里程碑行之后、`{% endif %}` 之前补三块图表区：

顶部加一行：

```jinja
{% from "insights/_cards.html" import toplist, comp %}
```

里程碑行 `</div>` 之后插入：

```jinja
<div class="card">
  <h3>逐年对比 <span class="muted card-sub">柱高即当年总花费 · 点击进入年度报告</span></h3>
  <div class="year-wrap"><canvas id="yearChart"></canvas></div>
</div>

<div class="overview-grid">
  <a class="card super-card super-most"
     href="{{ url_for('trips.detail', trip_id=s.most_expensive.id) }}">
    <span class="super-emoji">💸</span>
    <span class="super-label">最贵旅程</span>
    <span class="super-title">{{ s.most_expensive.title or '未命名' }}</span>
    <span class="super-amount">￥{{ '{:,.2f}'.format(s.most_expensive.total_cny) }}</span>
    {{ toplist(s.most_expensive, cat_colors) }}
    {{ comp(s.most_expensive, categories, cat_colors) }}
  </a>
  {% if s.cheapest %}
  <a class="card super-card super-cheap"
     href="{{ url_for('trips.detail', trip_id=s.cheapest.id) }}">
    <span class="super-emoji">🪙</span>
    <span class="super-label">最便宜旅程</span>
    <span class="super-title">{{ s.cheapest.title or '未命名' }}</span>
    <span class="super-amount">￥{{ '{:,.2f}'.format(s.cheapest.total_cny) }}</span>
    {{ toplist(s.cheapest, cat_colors) }}
    {{ comp(s.cheapest, categories, cat_colors) }}
  </a>
  {% endif %}
  <div class="card">
    <h3>整体消费构成</h3>
    <div class="donut-wrap">
      <canvas id="globalCat"></canvas>
      <div class="donut-center">
        <span class="donut-center-label">总计</span>
        <span class="donut-center-value">￥{{ '{:,.2f}'.format(s.grand_total) }}</span>
      </div>
    </div>
  </div>
</div>

<div class="card">
  <h3>各旅程花费对比 <span class="muted card-sub">柱长即总花费 · 分段为类别构成 · 点击进入详情</span></h3>
  <div class="compare-wrap" style="height: {{ 52 * s.trip_count + 70 }}px">
    <canvas id="compareChart"></canvas>
  </div>
</div>
```

`{% block scripts %}`——把 `trips/list.html` 现有的整段 `<script>`（第 117-332 行）原样搬来，只做三处改动：

1. `{% if overview.trip_count %}` → `{% if s.trip_count %}`
2. 顶部变量声明区补两行：
   ```javascript
   const years = {{ years_json | safe }};
   const BUSIEST_YEAR = {{ busiest_year | tojson }};
   ```
3. 在 `// ---- 整体消费构成环图 ----` 那段之前插入逐年图：

```javascript
// ---- 逐年对比：柱高=当年总花费，点击进年度报告，最忙那年描边高亮 ----
if (years.length) {
  new Chart(document.getElementById('yearChart'), {
    type: 'bar',
    data: {
      labels: years.map(y => y.year),
      datasets: [{
        data: years.map(y => y.total),
        backgroundColor: c => radialTint(c.chart.ctx, c.chart.chartArea, '#4a9fc9'),
        // 最忙的一年描金边，不靠颜色区分（色盲友好）
        borderColor: years.map(y => y.year === BUSIEST_YEAR ? '#d6a13d' : 'transparent'),
        borderWidth: years.map(y => y.year === BUSIEST_YEAR ? 2.5 : 0),
        borderRadius: 4, maxBarThickness: 46,
      }],
    },
    options: {
      maintainAspectRatio: false,
      animation: { duration: 800, easing: 'easeOutQuart' },
      transitions: { active: { animation: { duration: 0 } } },
      onHover: (e, els) => { e.native.target.style.cursor = els.length ? 'pointer' : 'default'; },
      onClick: (e, els) => {
        if (!els.length) return;
        const y = years[els[0].index];
        if (y.trip_count) window.location = '/insights/' + y.year;   // 空档年没得看
      },
      plugins: {
        legend: { display: false },
        tooltip: {
          displayColors: false,
          callbacks: {
            label: i => {
              const y = years[i.dataIndex];
              if (!y.trip_count) return ' 这一年没有旅程';
              return [` 总花费 ${fmt(y.total)}`,
                      ` ${y.trip_count} 趟 · ${y.days_on_road} 天在途`,
                      ` ${y.distance_km.toLocaleString('zh-CN')} km`];
            },
          },
        },
      },
      scales: {
        x: { grid: { display: false } },
        y: { beginAtZero: true, grid: { color: 'rgba(0,0,0,0.05)' },
             ticks: { callback: v => '￥' + Number(v).toLocaleString('zh-CN') } },
      },
    },
    plugins: [softShadow],
  });
}
```

**搬迁注意**：`Chart.defaults.plugins.tooltip` 那段里 `animation: false` 的注释必须原样带过来——tooltip 跟随动画在 3000+px 高的对比图上会让 hover 单帧飙到 20ms，这是踩过的坑。

`app/blueprints/trips.py` 的 `list()` 瘦身为：

```python
@bp.route("/")
def list():
    trips = Trip.query.order_by(Trip.start_date.desc()).all()
    return render_template("trips/list.html", trips=trips)
```

同时删掉现在已无人使用的 import（若 `trips_overview` / `trip_distance_km` 在 `trips.py` 里再无引用，从第 15-16 行移除；`trip_stats` 仍被 `stats_page` 用着，保留）。

`app/templates/trips/list.html` 整体重写为：

```jinja
{% extends "base.html" %}
{% block title %}旅程{% endblock %}
{% block content %}
<div class="list-head">
  <h1>我的旅程</h1>
  <a class="btn" href="{{ url_for('trips.create') }}">+ 创建旅程</a>
</div>

{% if trips %}
<ul class="trip-list">
  {% for t in trips %}
  <li class="card trip-item">
    <a class="trip-item-main" href="{{ url_for('trips.detail', trip_id=t.id) }}">
      <span class="trip-item-title">{{ t.title or '未命名' }}</span>
      <span class="trip-item-meta">
        {{ t.start_date }} ~ {{ t.end_date }}
        · {{ (t.end_date - t.start_date).days + 1 }} 天
        {% if t.cities %}· {{ t.cities | map(attribute='name') | join(' · ') }}{% endif %}
      </span>
    </a>
    <a class="trip-item-story" href="{{ url_for('trips.story', trip_id=t.id) }}">故事 →</a>
  </li>
  {% endfor %}
</ul>
{% else %}
<p class="muted">还没有旅程，点上面创建一个。</p>
{% endif %}
{% endblock %}
```

样式追加到 `app/static/style.css` 末尾：

```css
/* ---- 逐年对比图 ---- */
.year-wrap { height: 320px; position: relative; }

/* ---- 旅程列表（看板搬去 /insights/ 后的纯列表） ---- */
.trip-list { list-style: none; padding: 0; margin: 0; display: grid; gap: 10px; }
.trip-item { display: flex; align-items: center; justify-content: space-between; gap: 1rem; }
.trip-item-main { display: flex; flex-direction: column; gap: 3px; text-decoration: none; color: inherit; flex: 1; }
.trip-item-title { font-size: 1.05rem; font-weight: 600; }
.trip-item-meta { font-size: .82rem; color: #999; }
.trip-item-story { font-size: .85rem; color: #4a9fc9; text-decoration: none; white-space: nowrap; }
.trip-item-story:hover { text-decoration: underline; }
```

- [ ] **Step 4: 跑全量测试确认通过**

Run: `pytest -v`
Expected: PASS（全绿。特别确认 `tests/test_trips_crud.py::test_list_shows_trip` 仍通过——它只断言标题在页面上，瘦身后依然满足）

- [ ] **Step 5: 提交**

```bash
git add app/blueprints/insights.py app/blueprints/trips.py \
        app/templates/insights/overview.html app/templates/trips/list.html \
        app/static/style.css tests/test_insights_routes.py
git commit -m "feat: 逐年对比图 + 看板搬到足迹页，旅程列表页回归纯列表"
```

---

### Task 7: 地图取景开关 + 年度报告页

**Files:**
- Modify: `app/static/story-map.js:37-60`（`render` 的 opts）
- Create: `app/templates/insights/year.html`
- Modify: `app/blueprints/insights.py`
- Modify: `app/static/story.css`（追加到文件末尾）
- Test: `tests/test_insights_routes.py`

**Interfaces:**
- Consumes: Task 4 的 `lifetime.year_report(trips, year)`；`window.STORY_MAP.render(el, data, opts)`
- Produces: 路由 `insights.year_report`（`GET /insights/<int:year>`）；`STORY_MAP.render` 新增 `opts.frameAll`

- [ ] **Step 1: 写失败测试**

追加到 `tests/test_insights_routes.py`：

```python
def test_year_report_renders(client, app):
    seed(app)
    resp = client.get("/insights/2026")
    body = resp.get_data(as_text=True)
    assert resp.status_code == 200
    assert "2026" in body
    assert "东京之旅" in body
    assert "新解锁" in body            # 日本是 2026 首访
    assert "寿司" in body              # 年度之最：最贵的一顿饭
    assert 'id="year-map"' in body


def test_year_report_404_for_year_without_trips(client, app):
    seed(app)
    assert client.get("/insights/2020").status_code == 404


def test_year_report_year_nav(client, app):
    seed(app)
    body = client.get("/insights/2026").get_data(as_text=True)
    assert 'href="/insights/2025"' in body     # 上一年有数据
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest tests/test_insights_routes.py -k year_report -v`
Expected: FAIL — `/insights/2026` 返回 404

- [ ] **Step 3: 实现**

`app/static/story-map.js` 的 `render` 加 `frameAll` 开关。把第 37-56 行区间内这两处改掉：

原来：

```javascript
  function render(el, data, opts) {
    opts = opts || {};
```

改为（补一行注释说明这个开关的存在理由）：

```javascript
  // opts.frameAll：跳过 framingCities 裁剪，全部城市参与取景。
  // 裁剪规则是为「单趟旅程」调的（甩开出发地与远程中转），年度报告要看的是
  // 「那年走遍了哪」的全貌，一年多趟跨洲时裁剪会把画框框偏。
  function render(el, data, opts) {
    opts = opts || {};
```

原来：

```javascript
    const feat = { type: "FeatureCollection", features: framingCities(cities, home).map((c) => ({
      type: "Feature", geometry: { type: "Point", coordinates: [c.lng, c.lat] } })) };
```

改为：

```javascript
    const framed = opts.frameAll ? cities : framingCities(cities, home);
    const feat = { type: "FeatureCollection", features: framed.map((c) => ({
      type: "Feature", geometry: { type: "Point", coordinates: [c.lng, c.lat] } })) };
```

`app/blueprints/insights.py` 补 import 与路由：

```python
from flask import Blueprint, abort, render_template
from app.services.lifetime import lifetime_stats, year_report
```

追加路由（函数名 `year_report_page`，避免与导入的 `year_report` 重名）：

```python
@bp.route("/<int:year>")
def year_report_page(year):
    trips = Trip.query.order_by(Trip.start_date).all()
    r = year_report(trips, year)
    if r is None:
        abort(404)
    return render_template("insights/year.html", r=r, categories=CATEGORIES,
                           map_json=_safe_json(r["map"]),
                           cat_json=_safe_json({cat: float(v) for cat, v
                                                in r["by_category"].items()}))
```

创建 `app/templates/insights/year.html`：

```jinja
{% extends "base.html" %}
{% block title %}{{ r.year }} 年度报告{% endblock %}
{% block main_class %}story-main{% endblock %}
{% block head %}
<link rel="stylesheet" href="{{ url_for('static', filename='story.css') }}">
{% endblock %}
{% set cat_colors = {
  "吃饭": "#e76f51", "游玩": "#2a9d8f", "购物": "#e9c46a",
  "住宿": "#4a9fc9", "交通": "#9b8cb5", "其他消费": "#b0a99f"} %}
{% block content %}

{# ---- 第 1 节 年份封面 ---- #}
<section class="yr-cover">
  <div class="story-actions">
    <a class="story-back" href="{{ url_for('insights.overview') }}">← 人生足迹</a>
  </div>
  <div class="trip-title-nav">
    {% if r.prev_year %}
    <a class="title-nav-arrow" href="{{ url_for('insights.year_report_page', year=r.prev_year) }}"
       title="{{ r.prev_year }}">‹</a>
    {% else %}
    <span class="title-nav-arrow disabled">‹</span>
    {% endif %}
    <h1 class="yr-year">{{ r.year }}</h1>
    {% if r.next_year %}
    <a class="title-nav-arrow" href="{{ url_for('insights.year_report_page', year=r.next_year) }}"
       title="{{ r.next_year }}">›</a>
    {% else %}
    <span class="title-nav-arrow disabled">›</span>
    {% endif %}
  </div>
  <p class="yr-summary">
    {{ r.trip_count }} 趟旅程 · {{ r.country_count }} 个国家 ·
    ￥{{ '{:,.0f}'.format(r.total_cny) }} · {{ r.days_on_road }} 天在途 ·
    {{ '{:,}'.format(r.distance_km) }} km
  </p>
</section>

{# ---- 第 2 节 足迹地图 ---- #}
<section class="yr-section">
  <h2 class="yr-h2">这一年，你走过这些地方</h2>
  <div id="year-map" data-map='{{ map_json | safe }}'></div>
</section>

{# ---- 第 3 节 新解锁国家（没有就整节跳过）---- #}
{% if r.new_countries %}
<section class="yr-section">
  <h2 class="yr-h2">新解锁 {{ r.new_countries | length }} 个国家</h2>
  <div class="yr-flags">
    {% for c in r.new_countries %}
    <span class="yr-flag-item">
      <span class="yr-flag">{{ c | country_flag }}</span>
      <span class="yr-flag-name">{{ c }}</span>
    </span>
    {% endfor %}
  </div>
</section>
{% endif %}

{# ---- 第 4 节 旅程清单 ---- #}
<section class="yr-section">
  <h2 class="yr-h2">{{ r.trip_count }} 趟旅程</h2>
  <div class="yr-trips">
    {% for t in r.trips %}
    <a class="card yr-trip" href="{{ url_for('trips.story', trip_id=t.id) }}">
      <span class="yr-trip-title">{{ t.title or '未命名' }}</span>
      <span class="yr-trip-meta">{{ t.start_date }} ~ {{ t.end_date }}</span>
      {% if t.cities %}<span class="yr-trip-cities">{{ t.cities | join(' · ') }}</span>{% endif %}
      <span class="yr-trip-amount">￥{{ '{:,.0f}'.format(t.total_cny) }}</span>
    </a>
    {% endfor %}
  </div>
</section>

{# ---- 第 5 节 花费构成 ---- #}
<section class="yr-section">
  <h2 class="yr-h2">花了 ￥{{ '{:,.0f}'.format(r.total_cny) }}</h2>
  {% if r.prev_total_cny is not none and r.prev_total_cny > 0 %}
  {% set delta = (r.total_cny - r.prev_total_cny) / r.prev_total_cny * 100 %}
  <p class="yr-delta">
    比 {{ r.prev_year }} 年{% if delta >= 0 %}多{% else %}少{% endif %}
    {{ (delta | abs) | round | int }}%（{{ r.prev_year }} 年 ￥{{ '{:,.0f}'.format(r.prev_total_cny) }}）
  </p>
  {% endif %}
  <div class="donut-wrap">
    <canvas id="yearCat"></canvas>
    <div class="donut-center">
      <span class="donut-center-label">总计</span>
      <span class="donut-center-value">￥{{ '{:,.0f}'.format(r.total_cny) }}</span>
    </div>
  </div>
</section>

{# ---- 第 6 节 年度之最（四项全空就整节跳过）---- #}
{% set sup = r.superlatives %}
{% if sup.best_meal or sup.best_stay or sup.biggest_day or sup.farthest_trip %}
<section class="yr-section">
  <h2 class="yr-h2">这一年之最</h2>
  <div class="ms-row">
    {% if sup.best_meal %}
    <a class="card ms-card" href="{{ url_for('trips.story', trip_id=sup.best_meal.trip_id) }}">
      <span class="ms-emoji">🍜</span>
      <span class="ms-label">最贵的一顿饭</span>
      <span class="ms-value">{{ sup.best_meal.title }}</span>
      <span class="ms-sub">￥{{ '{:,.0f}'.format(sup.best_meal.cny) }} ·
        {{ sup.best_meal.date }} · {{ sup.best_meal.trip_title }}</span>
    </a>
    {% endif %}
    {% if sup.best_stay %}
    <a class="card ms-card" href="{{ url_for('trips.story', trip_id=sup.best_stay.trip_id) }}">
      <span class="ms-emoji">🏨</span>
      <span class="ms-label">最贵的一次住宿</span>
      <span class="ms-value">{{ sup.best_stay.title }}</span>
      <span class="ms-sub">￥{{ '{:,.0f}'.format(sup.best_stay.cny) }} ·
        {{ sup.best_stay.date }} · {{ sup.best_stay.trip_title }}</span>
    </a>
    {% endif %}
    {% if sup.biggest_day %}
    <a class="card ms-card" href="{{ url_for('trips.story', trip_id=sup.biggest_day.trip_id) }}">
      <span class="ms-emoji">💳</span>
      <span class="ms-label">花得最多的一天</span>
      <span class="ms-value">￥{{ '{:,.0f}'.format(sup.biggest_day.cny) }}</span>
      <span class="ms-sub">{{ sup.biggest_day.date }}{% if sup.biggest_day.city %} ·
        {{ sup.biggest_day.city }}{% endif %} · {{ sup.biggest_day.trip_title }}</span>
    </a>
    {% endif %}
    {% if sup.farthest_trip %}
    <a class="card ms-card" href="{{ url_for('trips.story', trip_id=sup.farthest_trip.trip_id) }}">
      <span class="ms-emoji">🛫</span>
      <span class="ms-label">走得最远的一趟</span>
      <span class="ms-value">{{ '{:,}'.format(sup.farthest_trip.distance_km) }} km</span>
      <span class="ms-sub">{{ sup.farthest_trip.title }}</span>
    </a>
    {% endif %}
  </div>
</section>
{% endif %}
{% endblock %}

{% block scripts %}
<script src="{{ url_for('static', filename='vendor/d3.min.js') }}"></script>
<script src="{{ url_for('static', filename='vendor/topojson-client.min.js') }}"></script>
<script src="{{ url_for('static', filename='story-map.js') }}"></script>
<script>
// 年度足迹图：复用故事页的 2D 小地图渲染器。frameAll 让全部城市参与取景——
// 默认的裁剪是为单趟旅程调的（甩开出发地与远程中转），年度报告要的是全貌。
(function () {
  const el = document.getElementById('year-map');
  if (!el || !window.STORY_MAP) return;
  const data = {{ map_json | safe }};
  // 等容器被 CSS 定尺再渲染（同 story-map.js 的 whenSized 兜底）
  let tries = 0;
  (function whenSized() {
    if (el.clientWidth >= 120 || tries++ > 30) {
      return window.STORY_MAP.render(el, data, { frameAll: true });
    }
    requestAnimationFrame(whenSized);
  })();
})();

const CAT_COLORS = {{ cat_colors|tojson }};
const CATEGORIES = {{ categories|tojson }};
const yearCat = {{ cat_json | safe }};
const fmt = v => '￥' + Number(v).toLocaleString('zh-CN',
  { minimumFractionDigits: 0, maximumFractionDigits: 0 });
const gLabels = CATEGORIES.filter(c => (yearCat[c] || 0) > 0);
const gValues = gLabels.map(c => yearCat[c]);
if (gLabels.length) {
  new Chart(document.getElementById('yearCat'), {
    type: 'doughnut',
    data: { labels: gLabels, datasets: [{
      data: gValues,
      backgroundColor: gLabels.map(l => CAT_COLORS[l] || '#b0a99f'),
      borderWidth: 2, borderColor: '#fff', hoverOffset: 12, borderRadius: 6, spacing: 2,
    }] },
    options: {
      cutout: '62%',
      animation: { animateRotate: true, animateScale: true, duration: 900, easing: 'easeOutQuart' },
      plugins: {
        legend: { position: 'bottom', labels: { padding: 12, usePointStyle: true, pointStyle: 'circle' } },
        tooltip: { displayColors: false, callbacks: { label: i => {
          const total = gValues.reduce((a, b) => a + b, 0);
          const pct = total ? (i.raw / total * 100).toFixed(1) : 0;
          return ` ${i.label}  ${fmt(i.raw)}  (${pct}%)`;
        } } },
      },
    },
  });
}
</script>
{% endblock %}
```

样式追加到 `app/static/story.css` 末尾：

```css
/* ---- 年度报告：沿用故事页的叙事排版语汇 ---- */
.yr-cover { position: relative; text-align: center; padding: 3rem 0 2rem; }
.yr-year { font-size: 5rem; line-height: 1; margin: 0; letter-spacing: .02em; }
.yr-summary { color: #666; font-size: 1.05rem; margin: .8rem 0 0; }

.yr-section { padding: 2.5rem 0; border-top: 1px solid #eee; }
.yr-h2 { font-size: 1.8rem; margin: 0 0 1.2rem; font-weight: 600; }

#year-map { height: 420px; background: #f7f5f0; border-radius: 10px; }

.yr-flags { display: flex; flex-wrap: wrap; gap: 1.5rem; }
.yr-flag-item { display: flex; flex-direction: column; align-items: center; gap: 4px; }
.yr-flag { font-size: 2.6rem; line-height: 1; }
.yr-flag-name { font-size: .9rem; color: #666; }

.yr-trips { display: grid; grid-template-columns: repeat(auto-fill, minmax(220px, 1fr)); gap: 12px; }
.yr-trip { display: flex; flex-direction: column; gap: 3px; text-decoration: none; color: inherit; }
.yr-trip:hover { transform: translateY(-2px); transition: transform .15s; }
.yr-trip-title { font-size: 1.05rem; font-weight: 600; }
.yr-trip-meta, .yr-trip-cities { font-size: .8rem; color: #999; }
.yr-trip-amount { font-weight: 600; color: #d65b3d; margin-top: .3rem; }

.yr-delta { color: #666; margin: -.6rem 0 1rem; }
```

- [ ] **Step 4: 跑全量测试确认通过**

Run: `pytest -v`
Expected: PASS（全绿；`tests/test_story.py` 也要绿——`story-map.js` 的改动只加了 opts 分支，默认行为不变）

- [ ] **Step 5: 手动验证**

```bash
python run.py
```

打开 `http://localhost:8000/insights/`，逐项确认：
- KPI 六个数字有值，在途天数与总里程不为 0
- 五张里程碑卡都在：第一次出国应是 **2012-09-18 马来西亚🇲🇾**
- 逐年图有 2012~2026 的柱子，**2020 与 2022 是空的**（补零），最忙的一年（2017，7 趟）有金色描边
- 点 2016 那根柱子 → 跳到 `/insights/2016`，页面上应能看到 `201612 - 台北` 那趟（跨年归 2016）
- 年度报告地图把那年所有城市都框在画面内（不是只框一趟）
- 打开 `http://localhost:8000/trips/` 确认列表页只剩列表
- 打开任意旅程的故事页，确认小地图取景与改动前一致（`frameAll` 默认关）

- [ ] **Step 6: 提交**

```bash
git add app/static/story-map.js app/static/story.css \
        app/blueprints/insights.py app/templates/insights/year.html \
        tests/test_insights_routes.py
git commit -m "feat: 年度报告页 + 地图全包取景开关"
```

---

### Task 8: 文档同步

**Files:**
- Modify: `ROADMAP.md:29-32`
- Modify: `DECISIONS.md`（追加，不改旧条目）
- Modify: `docs/specs/2026-06-30-travel-journal-design.md`（「功能总览」节）

- [ ] **Step 1: ROADMAP 勾选**

把「统计与洞察」两项改为已上线，格式对齐已完成项（故事页/演示模式那两条）：

```markdown
### 统计与洞察

- [x] **人生足迹总览页**：累计国家/城市/公里数/在途天数/总花费，里程碑（第一次出国、走得最远的一次、去得最多的城市），逐年对比。已上线（2026-07-17），设计见 [docs/specs/2026-07-17-insights-design.md](docs/specs/2026-07-17-insights-design.md)。
- [x] **年度报告**：某一年的旅行总结（旅程数、新解锁国家、花费构成、最贵的一顿饭……），天然适合导出分享。已上线（2026-07-17），设计同上。
```

- [ ] **Step 2: DECISIONS 追加**

追加到 `DECISIONS.md` 末尾：

```markdown
## 2026-07-17 · D21：跨旅程看板搬去足迹页；跨年旅程整趟归开始年

**背景**：第三版「统计与洞察」要做人生足迹总览页与年度报告。动手前发现**旅程列表页早已是个跨旅程消费看板**（总花费/旅程数/里程/国家/城市 Top5/最贵最便宜/消费构成/各旅程对比），与「人生足迹总览页」的清单大面积重叠——ROADMAP 那条里真正缺的只有在途天数、里程碑、逐年对比三块。

**决策**：
- **看板整体搬到 `/insights/`，旅程列表页回归纯列表**。理由是避免同一份 KPI 在两处维护；总览页成为唯一的「统计首页」。列表页只留标题 + 创建入口 + 旅程卡片。
- **两页共享 `services/lifetime.py`**：口径（年份归属、在途天数、新解锁国家）只在这一个模块定义一次，金额换算与里程分别复用既有的 `services/stats.py` 与 `services/distance.py`，不重写。
- **跨年旅程整趟归 `start_date` 的年份，不按天拆**。库里仅 1 趟受影响（`201612 - 台北`，2016-12-29 → 2017-01-02，跨 2 天）。按天拆虽更精确，但会让同一趟旅程同时出现在两年的报告里（「2017 年你走了 N 趟」里混进一趟 2016 年出发的），叙事别扭，且年度报告的旅程数与总览页对不上。为 1 趟旅程的 2 天不值得引入这个复杂度。
- **逐年对比空档年补零**：2020、2022 无旅程也画零柱——停摆本身是信息，补零才看得出疫情三年的形状。
- **年度报告地图复用故事页的 `STORY_MAP.render()`，但加 `opts.frameAll` 跳过取景裁剪**。那套裁剪（甩开出发地与远程中转，见 D18 时期的 story-map）是为单趟旅程调的，一年多趟跨洲时会把画框框偏；年度报告要的是「那年走遍了哪」的全貌。这是该渲染器的第三个消费方（故事页、放映壳、年度报告），opts 开关比分叉出第二个渲染器划算。
```

- [ ] **Step 3: 设计文档「功能总览」节同步**

`docs/specs/2026-06-30-travel-journal-design.md` 的「功能总览（当前已实现）」节做两处改动，其余节不动。

第 22 行「旅程管理」条目结尾的 `；旅程列表为跨旅程消费对比看板。` 改为：

```
；旅程列表为纯列表（跨旅程看板已移至人生足迹总览页）。
```

在第 28 行「旅程故事页」条目之后插入一条：

```markdown
- **人生足迹总览 / 年度报告**：`/insights/` 纵览全部旅程——六项累计 KPI（总花费/旅程数/在途天数/总里程/国家/城市）+ 五张里程碑卡（第一次出国、走得最远的一次、最长的一趟、去得最多的城市、离家最远的城市）+ 逐年对比柱状图（空档年补零，点柱进年度报告）+ 跨旅程消费看板（最贵/最便宜旅程、整体消费构成、各旅程对比）。`/insights/<year>` 是某一年的叙事式回顾，六节纵向滚动：年份封面 → 全年足迹地图 → 新解锁国家 → 旅程清单 → 花费构成与同比 → 年度之最。详见 [2026-07-17-insights-design.md](2026-07-17-insights-design.md)。
```

- [ ] **Step 4: 跑全量测试**

Run: `pytest -v`
Expected: PASS（文档改动不影响测试，这步是提交前的例行确认）

- [ ] **Step 5: 提交**

```bash
git add ROADMAP.md DECISIONS.md docs/specs/2026-06-30-travel-journal-design.md
git commit -m "docs: 同步足迹页与年度报告上线"
```

---

## 完成后

按 `superpowers:finishing-a-development-branch` 决定合并方式。合并前自检（CLAUDE.md 文档同步纪律）：spec 已随 Task 8 同步、ROADMAP 已勾选、DECISIONS 已追加、plan 做完即归档不再维护。
